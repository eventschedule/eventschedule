# WP Page Review Tracker

A checklist of every WP (marketing) site page, used to track review progress as each page is reviewed.

**Legend:** a checkmark in the **Reviewed** column marks a page as reviewed; a blank cell means it has not been reviewed yet. The **Updated** column tracks the current round of page updates: a checkmark means the page has been updated in this round, and a blank cell means it has not been reached yet. The column was cleared on 2026-09-10 to start the round, and each page's notes for it are appended to its Notes cell after the earlier record.

**Earlier rounds:** both finished 160 / 160, and the Notes cells keep their record. The first gave every page a page-exclusive, ground-up design (the Notes cell names it) and applied all 31 audience "For" page restyle briefs; the docs pages deliberately share one shell, so they got an accuracy and legibility pass instead. Pass 2, a deeper sweep for design, graphics and copy craft, finished 2026-09-04; its notes are prefixed `PASS 2:` and it is summarized below.

**Progress:** 161 / 161 reviewed

**Updated:** 161 / 161 updated

> **Round 3 - COMPLETE 2026-09-10.** Every page was re-read against the app as it ships after the
> features that landed since pass 2: PayPal and Payfast on every plan, refunds in full or in part
> through Stripe or PayPal, the interest list, the live calendar feed, email sign-ups and the
> automatic digest, the multi-event cart, installments, passes, gift cards, allocated seating,
> short links, Google Wallet, and the new appointment and analytics surfaces. What it found, in
> order of how much it mattered:
>
> **Tiers the code does not enforce, or enforces differently.** Event graphics were sold as Pro on
> dozens of surfaces, but `GraphicController` has checked membership only since January
> (0540a6b39): graphics are free, and only the AI-written text and the scheduled graphic emails
> are Enterprise. AI parsing was billed as Enterprise; it runs on every plan with a daily
> allowance (10 on a trial, 50, 100 on Enterprise). API gating, trials, team access and the
> check-in dashboard were corrected wherever a page disagreed with the gate.
>
> **Emails the hosted service does not send.** On eventschedule.com a schedule without its own email settings still sends
> buyers their ticket confirmation (the platform mailer has been the fallback since 2026-07-30),
> but sends no sale notifications, appointment emails, gift cards, pass booking confirmations,
> buyer change notices, feedback requests, or carpool and poll emails. `/features/ticketing` had
> it backwards on both counts; it and ten other pages now say which is which, and `docs/FEATURES.md`
> carries the requirement on every affected row.
>
> **Automatic versus chosen.** The interest list's change notice (sent only when the organizer
> chooses to notify on save), the digest's scope (only events the schedule created), a claimed
> talent schedule's approvals (always required) and several refund side effects were described as
> automatic or universal when they are not.
>
> **Competitor facts re-verified against the competitors' own pages**, not our old copy:
> Eventbrite's separate 2.9% processing fee per order (the /compare calculator now includes it),
> Luma's coupons, webhooks, calendar embeds, unlisted events and clone-only repeats, Accelevents'
> and AddEvent's pricing, Calendly's booking payments, Squarespace's own email campaigns and more.
> Cells that could not be verified are named in the row notes rather than changed.
>
> **SEO, measured on all 161 pages**: titles 30 to 63 characters (the four under 45 are the three
> legal pages and the translated accessibility statement), descriptions 130 to 160, no duplicate
> titles or descriptions, one H1 each, valid JSON-LD, FAQPage entries matching the visible FAQ, no
> em-dashes. The docs had 18 titles under 40 characters; each now says what the page covers.
>
> **Guard added**: `MarketingTicketingTierTest` also catches the price-then-ticketing shapes
> ("with ticketing at $5/month", "the Pro plan at $5/month, which also covers ticketing") that got
> past it, checked against the fixture they shipped in.
>
> **Code problems found along the way** are for the owner, not the copy, and are listed in the
> round's final report: WhatsApp event creation has no Enterprise gate, webhooks are never retried,
> API rate limits count per IP on a window that never resets under steady traffic, the web editor
> does not lock an event's currency after it has taken money, and a selfhost never emails buyers
> about a change or cancellation.

> **Pass 2 - COMPLETE 2026-09-04.** The second sweep went page by page with the whole site in
> view. What it actually found, in order of how much it mattered:
>
> **Claim rot that the first sweep's greps could not see.** Thirteen more surfaces still said
> selling tickets is a paid feature, in thirteen different wordings ("Selling covers is on the
> Pro plan", "Ticketing with QR check-in ... are on the Pro plan", "QR code tickets are included
> in the Pro plan", and a price-first shape with no plan name in it at all: "$5/month for
> ticketing"). Eleven more still said nothing is mailed automatically, which
> `app:send-event-announcements` invalidated - including `/features/newsletters`, the page whose
> entire subject is who hears from a schedule, denying the one thing that sends itself. Six more
> promised team collaboration on the free plan. Every one was rewritten in its own page's voice.
>
> **Three regression tests now pin all of it**, and each assertion was A/B'd by reintroducing the
> claim it targets: `MarketingTicketingTierTest`, `MarketingAudienceClaimTest` and the extended
> `MarketingCountedClaimsTest`. They read the RENDERED `tests/fixtures/*.json` as well as the
> source, because the controller concatenates `plan_price()` into the middle of these sentences
> and a source-only scan reads straight past them.
>
> **Measured, not assumed**, across all 157 public pages: 0 contrast failures in light AND dark,
> 0 pages scrolling horizontally at 390px, 0 targets under the WCAG 2.2 24px minimum, 0
> interactive elements without an accessible name, 0 dangling fragment links out of 2,406, and a
> reduced-motion guard on every animated page. `/accessibility` publishes that record now instead
> of a good-faith undertaking alone, translated into all twelve languages.
>
> **Structural work that lifted whole families at once**: a real four-step migration path on all
> sixteen comparison pages (it had been "sign up, add events, share your URL"); the shared plan
> band on six feature and integration pages that named tiers but never priced them; a Last
> updated date and an Improve this page on GitHub link on all 37 docs pages, read from the same
> manifest entry their JSON-LD already used; 377 decorative glyphs marked `aria-hidden`.

> **Ticketing plan model change - SWEPT 2026-07-31.** Another session shipped a real product
> change mid-campaign: the FREE tier now SELLS paid tickets, 25 per calendar month per
> schedule (`Role::ticketSaleLimit()`, `config('usage.ticket_sale_monthly_limit_free')`) with
> a 50/month per-owner backstop. Selfhost, demo schedules and Pro/trialling are unlimited;
> non-addon zero-price tickets and events starting within 48 hours are exempt
> (`Event::hasTicketAllowance()`). **Pro now means UNLIMITED ticket sales, not "ticketing".**
> Still Pro: QR check-in dashboard, individual tickets, passes, waitlist, promo codes,
> add-ons, sales CSV export, gift cards, bulk import, the ticket-purchase embed.
>
> A sentence-level scan found the stale claim on **21 pages** in three different phrasings
> ("is on Pro", "are five dollars a month", "is $5 a month") - token greps had under-counted
> it twice. All 21 were corrected one page at a time, each rewritten in its own page's voice
> rather than by find/replace, which mangled prose here on a previous occasion. `/pricing` was
> already correct and served as the reference; `contact`, `docs/subscriptions`,
> `docs/selfhost/index` and `docs/analytics` were verified correct and left alone.
>
> Two further plan facts surfaced during the sweep and were fixed in passing: **appointment
> booking is now FREE** (`Role::appointmentTypeLimit()`, one type free) and **volume discounts
> were never Pro** (`volume_discount` is not in EventRepo's `$ticketExtrasAllowed` scrub).
> `for-musicians` also carried the long-standing "newsletters are available on the Pro and
> Enterprise plans" error - newsletters are FREE at 10 recipients/month - now corrected.
>
> **Door scanning is FREE - resolved and swept 2026-07-31.** The product owner decided free
> users may scan the 25 tickets a month the free plan sells. **No code change was needed:**
> `TicketController::scan()` and `scanned()` carry no plan gate at all (`scan()` even has a
> comment saying it is available on every plan because the Pro feature is the dashboard),
> `User::canScanEvent()` is a permission check for owners/admins/viewers with no `isPro()`,
> and the "Scan Ticket" link in `ticket/sales.blade.php` renders outside the `@if` that guards
> the dashboard link. The live **check-in dashboard** (`CheckInController::index/stats`,
> running count and per-ticket-type breakdown) stays Pro, which is exactly what `/pricing`
> already said.
>
> `docs/FEATURES.md` was the source of the error: it listed "QR code check-ins" under **Pro**
> and claimed "the scan chokepoint is `User::canScanEvent()`" - a function with no plan check.
> That row is now a **Free** row, "QR code scanning at the door", citing the real code path;
> the separate "Check-in dashboard" Pro row is untouched. Then 20 marketing and docs pages
> were corrected one at a time. Several kept a half that is still true - passes, promo codes,
> add-ons, waitlist, custom fields and the live count all remain Pro - so this was never a
> blanket replace. The sharpest result is on `/for-libraries`: "Scanning the QR on a ticket is
> free on every plan; it is the running total that is Pro."

> **Feature-claim accuracy audit (2026-07-31): 305 contradictions found and fixed.** The first
> site-wide check of WP copy against what the app actually does, prompted by the fact that
> accuracy had only ever been checked per page, at different times, against a `FEATURES.md`
> that changed twice mid-campaign. A polarity-aware checker (`audit-claims.py`) ran over all
> 151 pages - it has to be polarity-aware because these pages are full of deliberate honesty
> statements ("there is no seat map") that a naive grep reports as defects. Then 25 high-risk
> pages got a per-page agent required to cite the model, column or code path behind every
> capability claim, or delete it.
>
> The worst finding was on `/for-circus-acrobatics`, a page that had already been rebuilt
> *with* a documented fabrication sweep: a mock streaming player with a LIVE badge,
> "847 watching", a live chat feed of invented viewer comments, and "$25 tip from Sarah!".
> **No tipping code, no chat model and no viewer counter exist anywhere.** Also removed: the
> homepage newsletter mock showing a send of 1,248 recipients (impossible - the Enterprise
> ceiling is 1,000), "Followers get notified when you add events" on `/use-cases` (there is no
> automatic follower notification at all), and a claim that one extra team member is free
> (`RoleController::createMember()` hard-returns unless `isEnterprise()`).
>
> Result: **0 nonexistent-feature claims and 0 impossible numbers site-wide.** Two categories
> are left deliberately unfixed because they are risks rather than present contradictions:
> 5 pages hand-roll `FAQPage` JSON-LD instead of `<x-seo.faq-schema>` (verified on the
> rendered HTML - every JSON-LD question does appear visibly today, so nothing is drifting),
> and 57 pages hardcode "$5"/"$15" while `/pricing` reads them from
> `config('services.stripe_platform.*')` (all correct today; they only go stale if the price
> changes).

> Scope: static and functional marketing pages served under `marketing.*` routes (`routes/web.php`, `MarketingController`), cross-checked against `resources/views/sitemap.blade.php`. Excludes URL redirects, the shared partials/components, and individual blog posts. The comparison and replacement detail pages each render one shared template driven by per-slug data.

## Main / Top-level (14)

| Reviewed | Updated | Page | URL | Notes |
|:--------:|:-------:|------|-----|-------|
| ✅ | ✅ | Homepage | `/` | Homepage-exclusive design (poster wall, 3D showcase, pinned gallery rail, integrations orbit). 2026-09 pass: reserved seating + gift cards added to the grid, calendar/analytics/AI/ticketing claims re-grounded, FAQ expanded to 7 with payouts and custom domains, how-it-works corrected for automatic new-event digests, claim input no longer a dead end. PASS 2: contrast clean in both modes (accent "Learn more" links were 3.2-4.1:1 on white); the twelve-colour link palette in the feature grid unified on one brand blue so the icon chips carry identity and the affordance reads as one thing; captions inside the mockups raised off gray-400. ROUND 3: Title and meta now say what the product is (a free event calendar and ticketing platform that sells 25 paid tickets a month). PayPal joins every Stripe-only line (the payments footer, the integrations lede, the 0% card and the JSON-LD, which gained a featureList); the interest list joins the ticketing card and the live calendar feed joins how-it-works. Corrected against the code: the FAQ called Invoice Ninja Pro and implied add-ons came with free selling, the sync card promised CalDAV deletions it cannot make, recurring said weekly only, and "Keep 100%" ignored processor fees. The FAQ grew to 10 (refunds, the interest list, pages for the acts and venues you list). |
| ✅ | ✅ | Features | `/features` | Five chapters via `<x-marketing.feature-chapter>` / `<x-marketing.feature-banner>`. 2026-09 pass: new "small print" section covering 16 real capabilities the page never named (installments, add-ons, waitlist, bulk import, WhatsApp, agenda scanning) with per-row tier badges; private-events banner rebuilt around the four real visibility states; newsletters, fan content and analytics claims re-grounded. PASS 2: contrast clean in both modes; the shared feature-banner and feature-chapter accents (nine hues each) raised off -600, which fixes every page that renders them; the "small print" grid`s link colour unified. ROUND 3: The ticketing lede said "Accept payments with Stripe" and read as if the waitlist and live dashboard were free; it now carries the 25-a-month allowance, the six payment methods, refunds and the Pro half, and the check-in card separates free scanning from the Pro dashboard. The small print grew from 16 to 24 rows for what shipped after 2026-09-05 (PayPal, refunds, the interest list, Add to Google Wallet with its operator caveat, the live feed, sign-up with an account, short links, pages for listed acts, the whole lineup), each badged against docs/FEATURES.md and linked. Title and meta name PayPal and zero platform fees; three FAQs added. |
| ✅ | ✅ | Pricing | `/pricing` | Rebuilt July 2026 (the feature lists are curated - see CLAUDE.md before editing them). 2026-09 pass: added the two questions asked most before signing up - what happens at the 25-ticket cap, and what happens on cancel or downgrade - answered from `Role::ticketSaleLimit()` / `paidTicketAllowanceAvailable()` behaviour; fee answer rewritten around direct payouts; fixed a hardcoded `$` beside our own Pro price in the fee calculator. PASS 2: each plan CTA now carries its own reassurance line - no card on Free, seven days then the price and what cancelling does on Pro, the selfhost alternative on Enterprise - which the page previously stated above the price, before the reader had decided anything. Curated lists untouched. ROUND 3: The meta description still said "Upgrade to Pro for ticketing" in all twelve locales, a wording MarketingTicketingTierTest cannot see; rewritten around the free 25 paid tickets a month. FAQs added for the payment methods (all six on every plan), refunds and the free interest list; the trial (once per schedule), downgrade (the ticket window restarts the day the plan ended; Internal and Unlisted become Drafts on their next save) and Pro-versus-Enterprise answers re-verified and corrected. Curated plan lists untouched; their gaps are listed for the owner in the round summary. |
| ✅ | ✅ | About | `/about` | "The Colophon" ground-up rebuild. 2026-09 pass: errata corrected now that `app:send-event-announcements` ships - the automatic digest reaches confirmed subscribers, while account followers stay newsletter-only, and the double-booking erratum now excludes appointments, which genuinely cannot clash. PASS 2: a sixth erratum, which is the section this page exists for - "unlimited events" has a daily ceiling on the hosted site (Role::eventCreateDailyLimit: 500 a day per schedule, 1,000 on Enterprise, 100 during a Pro trial, with a wider per-owner ceiling), a rate limit rather than a plan feature and absent on a selfhosted install. The standfirst's count is now derived from the array, so it cannot drift again. ROUND 3: Three payment-route lines still said Stripe only and now name PayPal. The errata grew to a derived eight with two limits read from the code: refunds move money only on Stripe and PayPal and never email the ticket buyer, and PayPal takes ticket orders but not installments, gift cards or appointments. Erratum 4 no longer calls the digest the only mail outside the newsletter count, erratum 2 now covers only a signed-in Follow, seat maps say the room is drawn on a venue schedule, and the meta description (12 locales) names what the page holds. |
| ✅ | ✅ | Demos / Examples | `/examples` | "The Showroom" ground-up rebuild; route name is `marketing.demos`. 2026-09 pass: all 18 demo schedules re-verified live (HTTP 200), and the spec sheet now says the Follow button earns an automatic new-event digest, not just a list to write to. PASS 2: all 18 demo schedules re-checked live, and the spec sheet`s Follow row corrected for the automatic digest. ROUND 3: The spec sheet now covers what a visitor can do today: the whole lineup (with a claimable page for an act not on Event Schedule), the interest list and the live feed of the whole schedule. The Follow row says only confirmed sign-ups get the digest, checkout names Stripe or PayPal, and step 03 describes the automatic digest. All 18 demos re-checked against the controller and DemoService. |
| ✅ | ✅ | Search | `/search` | "The Lookup" ground-up rebuild; functional page, search form and query handling preserved. 2026-09 pass: the "when you sell a ticket" row was badged Pro while the page FAQ said Free two sections later - selling is free to 25 paid tickets a month, so the badge and copy now agree; follower row corrected for automatic digests. PASS 2: the meta description was a generic "find fitness classes, music venues, community groups"; it now states the lookup rules the page itself documents - what is matched, the two-character minimum and that past dates never come back. ROUND 3: The not-found FAQ explains that a page created for an unclaimed act stays out of this index and out of search engines until claimed. The audience bento says the digest needs a confirmed address and adds the interest list and the live feed; ticketing names Stripe or PayPal. Title and meta rewritten. The page promised "soonest first" while MarketingController::search() sorted a recurring series by the date it began; the query now uses browse()'s ordering, so the copy is true. |
| ✅ | ✅ | Browse | `/browse` | "The Newsstand" ground-up rebuild; functional page, filters and query params preserved. 2026-09 pass: added the question organizers actually ask - how an event ends up on the page - answered from `MarketingController::browse()` (public + a flyer, or a talent/venue profile photo, 24 shown). PASS 2: the hero now credits the people publishing rather than describing a container, and the page renders clean at 390px with zero contrast failures. ROUND 3: Added a visitor FAQ for an event whose tickets are not on sale yet (the interest list: email only, per date, one-click unsubscribe); the account answer lists what needs no account (calendar, the feed, the interest list); the organizer answer now matches browse() exactly, including the confirmed email or phone publicUpcomingEventsQuery() requires. Title retargeted to "Upcoming Events: Music, Comedy, Classes". |
| ✅ | ✅ | FAQ | `/faq` | "The Front Desk" ground-up rebuild. 2026-09 pass: rate card split QR scanning (free on every plan) out of the Pro check-in dashboard row, and three answers that still called ticketing a paid feature were corrected against `Role::ticketSaleLimit()`; 05.03 rewritten now that `app:send-event-announcements` reaches confirmed subscribers. PASS 2: the "39 answers in 9 corridors" claim re-counted against the rendered page (39 summary elements); rate card and answers already corrected in pass 1. ROUND 3: Stale answers corrected: checkout said Stripe only, the sold-out answer called every waitlist Pro (the RSVP waitlist is free), Enterprise never mentioned reserved seating, and the rate card said thirteen rows while showing fifteen (the count is now computed). Five questions added: PayPal, refunds and the interest list in corridor 03, the sign-up that creates an account in 05, claiming a page made for you in 06; the calendar answer gained the live feed and the security answer covers every gateway. Now 44 answers in 9 corridors. |
| ✅ | ✅ | Why Create an Account | `/why-create-account` | "The Keyring" ground-up rebuild. 2026-09 pass: two more genuinely open doors added (hearing about new events via the guest sign-up panel, and submitting an event), keeping the grid at 2x3, and the ledger`s Follow row now separates the Follow button, which needs an account, from leaving an email address, which does not. PASS 2: its follow answer said nothing about following is automatic, which is true of an ACCOUNT FOLLOWER and false of the email panel the same page describes two sections earlier. Rewritten to name both lists, and MarketingAudienceClaimTest gained the shape that carried it ("no notification that fires") plus a sentence-scoped subject test, because the span-scoped one had exempted it. ROUND 3: The Follow row, the Follow key and the not-a-subscription card went stale when a signed-out Follow became the sign-up form and confirming it began creating an account; all three rewritten. The open doors now include the email-only interest list and the live feed (grid kept at 2x3), the ledger gained "Claim a page made for you" with derived counts, two FAQs were added, and the title and description (12 locales, messages.php) now ask whether you need an account and what a free one adds. Coordinator: the interest-list answer said guests get "one [email] if the date or venue changes"; a change notice goes only when the organizer chooses to send one on save, so it now says "plus any notice the organizer chooses to send if the date or venue changes". |
| ✅ | ✅ | Use Cases | `/use-cases` | Component-driven directory via `<x-marketing.audience-card>`. 2026-09 pass: all 31 audience links verified against the route table; the curator "Build Your Audience" tile now states the automatic new-event digest, with the subscriber-vs-follower distinction pinned in a comment so it cannot drift back. PASS 2: the hero was a category label ("Built for every kind of organizer") on a page whose whole point is the range; it now names the range. All 31 audience links re-verified. ROUND 3: The sell-tickets FAQ named Stripe only; it now lists every gateway on every plan with the 25-a-month allowance and free door scanning. The curator card gained claimable pages for listed acts and the live calendar feed (a complete 3x2 grid), the digest tile names confirmed subscribers, the talent lede adds Outlook and CalDAV and a claim line, and a new FAQ covers listing an act who is not on Event Schedule. Coordinator: the developer Webhooks card carried an "Automatic retries" tag, but deliveries are never retried (dispatchAfterResponse on the sync connection); the tag now reads "Fourteen event types". |
| ✅ | ✅ | Contact | `/contact` | "The Postcard" ground-up rebuild. 2026-09 pass: added a private security-disclosure row to the routing table (the page previously sent "something is broken" straight to a public issue tracker with no stated alternative), and the two-of-five self-service note now reads six. PASS 2: carried the stale "selling is Pro" claim in its own wording, which the Pass 1 sentence scan did not match; corrected against Role::ticketSaleLimit() (25 paid tickets a month free, scanning never gated) and now pinned by MarketingTicketingTierTest. Decorative glyphs marked aria-hidden. ROUND 3: The routing table gained the two messages the new features will send here: a refund or event question goes to the organizer (the money is in their own Stripe or PayPal account), and a page in your name you did not make goes to its Claim this page and This is not me buttons; the address count is now derived. Matching FAQs added; the title and meta name the support email, bug reports and refunds. |
| ✅ | ✅ | Open Source | `/open-source` | "The Commit Log" ground-up rebuild. 2026-09 pass: the hosted-vs-selfhost diff billed QR check-in as Pro when scanning has no plan check at all - the row is now the check-in dashboard. API surface (24 authenticated endpoints), OpenAPI counts (16 paths, 26 operations) and the four machine-readable files re-verified against `routes/api.php` and `public/api/openapi.json`. PASS 2: its counted claim is now pinned by MarketingCountedClaimsTest, which asserts the number against the code it counts and names the source in the failure message. (Twenty-four authenticated endpoints and the 16/26 OpenAPI shape, against `routes/api.php` and the published spec.) ROUND 3: Three stale claims fixed against the code: the selfhost import is a daily import from a list of event URLs (not a city search), a licence credit stays on selfhosted public pages, and per-schedule custom domains are hosted-only. The API table says PUT /api/sales/{id} marks paid, cancels or refunds (in full or in part), PayPal joins the payout lists, and the H1 carries "Open source". The 24-endpoint, 16/26 OpenAPI and four-file counts re-verified unchanged. |
| ✅ | ✅ | Selfhost | `/selfhost` | "The Terminal" ground-up rebuild. 2026-09 pass: the "yours to run" list gained the two responsibilities selfhosters discover late - outbound SMTP, which the setup wizard deliberately does not write, and disk for uploaded flyers, photos and generated graphics - and the grid moved to 3-wide so six cells fill cleanly. PASS 2: contrast clean; the terminal prompt green moved to emerald-700 and its path opacity raised from 0.55 to 0.72, and the inline `APP_URL` chip got its own ink rather than inheriting a muted paragraph; the hero moved off "Your server, your rules" to say what selfhosting actually gets you. ROUND 3: PayPal added wherever the page only talked about Stripe (the checkout list, data panel and JSON-LD), with an FAQ on payment methods (the install-wide PayPal default, refunds, no public host needed except the optional webhook) and one naming every integration that needs the operator's own account, Google Wallet included. Two honesty lines corrected now that translation sharing exists (Federation is no longer "the one bridge back"). PayPal checkout and allocated seating joined the marquee; the docs map links the Google Wallet guide. |
| ✅ | ✅ | SaaS | `/saas` | "The Stack" ground-up rebuild; white-label SaaS operator landing. 2026-09 pass: added the three operator revenue rails the page never mentioned (AdSense on your free tier, prepaid promotions between your tenants, the Stay22 accommodation affiliate) with their real gates and exclusions, linked to `/docs/saas/monetization`; timeline pills no longer wrap in the third comparison card. PASS 2: contrast clean; the tenant-page mock stays light in both colour modes and was carrying dark: text variants (blue-300 on blue-50 at 1.66:1) - removed; the revenue sliders are now 24px tap targets with the same 6px track; money figures moved to amber-700. ROUND 3: Tenants were described as selling only through Stripe Connect; the admin-portal copy, feature dock and JSON-LD now include each tenant's own PayPal account and refunds. New FAQs on where ticket money goes (every rail is the tenant's own and nothing in checkout takes a cut) and on the admin panel (plans, promotions, domains, releasing and restoring a squatted subdomain); the hosting FAQ notes that Google Wallet issuer credentials switch on passes for every tenant's buyers. |

## Feature pages (39)

| Reviewed | Updated | Page | URL | Notes |
|:--------:|:-------:|------|-----|-------|
| ✅ | ✅ | Ticketing | `/features/ticketing` | "The Turnstile" ground-up rebuild. 2026-09 pass: added installments and the multi-event cart to the sale side (both real checkout behaviour the page never named), and the sale-notification card now states the first paid sale on an event always notifies, on any plan. PASS 2: reviewed at 1440 and 390; the duplex layout, perforated centre rule and both eight-item halves hold up, contrast is clean in both modes, and the sale side now carries installments and the multi-event cart added in pass 1. ROUND 3: The duplex is now ten and ten by design: the sale side adds the interest list, Stripe or PayPal checkout and refunds that move money; the door side adds Add to Google Wallet and change and cancellation notices. Stripe-only wording in the fee section and two FAQs corrected, PayPal and Payfast joined the payment grid (3x2), the refund FAQ matches SaleRefundService, and the newsletter allowance is stated in recipients. Title "Event Ticketing with Zero Platform Fees" in all 12 locales; Keep reading links /features/passes and /features/check-in. Coordinator (hosted email pass): the confirmation-email copy said nothing leaves until you add your own sender; since 2026-07-30 a buyer's confirmation leaves from our address until you do, so that is what it says now. The sale-notification, feedback-request and gift-card claims (including "the first paid sale always notifies you") now say they need the schedule's own email settings on eventschedule.com, FAQ JSON-LD included. |
| ✅ | ✅ | Gift Cards | `/features/gift-cards` | "The Gift Envelope" ground-up rebuild. 2026-09 pass: every number re-verified in code (max:12 denominations, max:500 message, redeem-after-promo ordering) and the checkout section gained the one behaviour it was missing - a card pays for a whole multi-event cart in one go, not event by event. PASS 2: reviewed; the seven states, twelve denominations and 500-character message all re-verified in code, and the multi-event cart fact added in pass 1. ROUND 3: Never said that PayPal, which now sells tickets, still cannot sell a gift card; How they pay and a new FAQ say so, and that a card bought another way still spends on a PayPal event. The card's own Refund action is marked as moving no money, a full ticket refund (not only a cancellation) puts the card's share back, and a new FAQ walks the page's own Sunday Matinee order through a refund. Title gained "A Balance, Not a Coupon". |
| ✅ | ✅ | Allocated Seating | `/features/allocated-seating` | Reserved-seating landing page (was missing from this tracker entirely). 2026-09 pass: the 12-minute hold and the orphan-seat rule re-verified against `SeatHoldService::HOLD_SECONDS` and `OrphanSeatRule`; two FAQs added for the two things that surprise people - plans are built on VENUE schedules only, and a plan already attached to an event survives the schedule lapsing. PASS 2: the box office section promised "the same map, with the names on" and then showed four text cards. It now shows the console: the hero`s seat geometry with one seat selected and the booking record beside it, plus the three actions. The page was the thinnest of the feature set at 551 lines and had no visual after the hero. ROUND 3: Added the Aug 27 seating UX work a venue buyer was missing: stage markers and labels, the per-room single-seat rule (with an FAQ), and a box office grown to six cards (hold or release a row or a whole section, arrivals stamped by the door scan, every night of the run with date pickers and a run summary). The release card no longer says "refund in your payment provider as usual": releasing moves no money, and a Stripe or PayPal share is a partial refund from Sales; a new FAQ says a full refund returns every seat. Meta and featureList carry best-available picking. |
| ✅ | ✅ | Passes & Subscriptions | `/features/passes` | "The Punch Card" ground-up rebuild, 2026-09-09. Built because `features.blade.php` marketed passes only as a deep link into `/docs/subscriptions`. The card is a fixed-in-both-modes physical object (band-diff: 63 snapshots, 0 differences, positive control firing) and the holes carry the visit count, which is the data model. Every claim taken from `/docs/subscriptions` and `docs/FEATURES.md`: four pass types, one QR with one counter, max-per-order pinned at 1, coverage resolving inside the selling schedule, advance booking sharing one seat pool, the cancellation deadline with forfeit-or-block, and admissions counted separately from visits. Teal #115e59 / #5eead4 - KNOWN AND ACCEPTED that `/caldav` also paints #115e59; different family, neither in the other's related strip. ROUND 3: docs/NEXUS_RELEASE.md said this page advertises Google Wallet, but it never mentioned it; the door section now has "One wallet pass, not one per date" (a pass saves as a single undated Wallet pass carrying the QR, the admissions count and the valid-until date), echoed in the QR FAQ and the featureList. Refunds became their own FAQ matching the code: Stripe or PayPal, full or partial; a partial refund leaves the pass working, a full one stops it scanning and releases its booked dates. Coordinator (hosted email pass): the booking-confirmation email is qualified as needing the schedule's own email settings on eventschedule.com. |
| ✅ | ✅ | Check-in Dashboard | `/features/check-in` | "The Head Count" ground-up rebuild, 2026-09-09. Also previously a docs deep link. The page leads with the free/Pro split because we have shipped that backwards before: door scanning has NO plan check (`TicketController::scan()` / `User::canScanEvent()`) and only the live dashboard is Pro (`CheckInController`). Deliberately achromatic - every hue in the feature family was spent - so the identity is the figure itself; the ops screen is fixed in both modes. Ten-second foreground-only refresh and venue-date keying both stated, since both are the sort of detail a reader assumes wrongly. ROUND 3: One refund statement fixed: a partial refund keeps the tickets valid, so the wallet answer says "fully refunded". A new FAQ lists exactly what the scanner turns away (unpaid, cancelled, fully refunded, expired, outside the scan window) and the amber hold for an installment plan that is behind. Title leads with "QR Ticket Check-in" and states the free/Pro split; the guide link goes to /docs/tickets#check-in. |
| ✅ | ✅ | Promo Codes & Add-ons | `/features/promo-codes` | "The Order Line" ground-up rebuild, 2026-09-09. Two `$alsoIncluded` rows that had no page anywhere. Both features share a page because their interaction is the most misunderstood thing about either: a code NEVER discounts an add-on, and it is applied after any volume discount. The hero is a receipt whose indent makes that ordering visible rather than stated (80 - 8 = 72, 20% = 14.40, + 12 add-on = 69.60; the arithmetic is self-consistent). Review added a definition of "volume discount", which the page named in the receipt and never explained. ROUND 3: Nothing was made wrong by the refund, PayPal or cart work, so refunds stay off the page. A new FAQ and featureList line say a code belongs to the event it was made on (the same code can be added to several) and in the multi-event cart each event's code discounts only its own tickets. New title and trimmed description; the guide link goes to #promo-codes. |
| ✅ | ✅ | Ticket Waitlist | `/features/waitlist` | "Now Serving" ground-up rebuild, 2026-09-09. The tier split is the free RSVP branch versus the Pro ticket branch, verified in `event/show-guest.blade.php:1051-1072`: it is one button with a conditional label, and the RSVP branch carries no `isPro()` check. Drawn as a queue rather than a list because one offer is live at a time - a list has no head, and picturing a broadcast would argue against the page's own mechanism. Review narrowed the meta description, which claimed a sold-out night "can never be oversold" when the one-at-a-time rule only guarantees a returned seat is never promised twice. ROUND 3: The refund card and FAQ predated partial refunds: only a refund in full frees the seat and wakes the queue. A new FAQ separates the waitlist from the new interest list (email only, free, never offers a seat) and links /docs/tickets#interest-list. Title "Ticket Waitlist for Sold-Out Events", keeping the pass-1 correction that a returned seat is never promised twice. |
| ✅ | ✅ | Installment Payments | `/features/installments` | "The Standing Order" ground-up rebuild, 2026-09-09. Pro and Stripe-only, because Stripe is the only rail that can charge a saved card off-session. Every setting taken from `/docs/tickets`: the odd cent on the first payment, the runway before the doors, the order-value floor, and the reasons the option disappears (Stripe's minimum charge per leg, a multi-event basket, a pass). **Review caught a real factual error here** - the page said the editor "refuses" to let you build a plan that finishes after the event. It warns; the enforcement is at checkout, where `InstallmentService::ineligibleReason()` returns `installments_does_not_fit`. Fixed, and the page had already said the correct version twice elsewhere. ROUND 3: Refund and failed-payment copy now matches the app: a plan is refunded in full only, leg by leg through Stripe from the Sales page, which cancels the payments still to come and returns the seats; a declined card is retried, and an overdue ticket shows the door the name and balance instead of a flat refusal, with paying or a new card lifting the hold. The PayPal answer says the other methods still sell the same tickets in one payment. The H1 opens "Pay in installments". |
| ✅ | ✅ | AI | `/features/ai` | "The Spark" ground-up rebuild. 2026-09 pass: field list, venue-matching ladder, allowances and the WhatsApp path all re-verified in code and already exact; added the one path the page never covered - `EventController::guestParse` gives visitors filling your public submission form the same parser, metered against your allowance rather than theirs. PASS 2: its counted claim is now pinned by MarketingCountedClaimsTest, which asserts the number against the code it counts and names the source in the failure message. (Twelve languages, against `config('app.supported_languages')` - the string lives in `marketing.php`, which the first draft of the test failed to read.) PASS 2: plan band added before the related-pages rail. Decorative glyphs marked aria-hidden. ROUND 3: Rung 05, the performers card and a new FAQ now say what happens to a name that matches nothing: saving creates a public page for the act or venue, credited per date, noindex until claimed, with This is not me, linked to /docs/creating-events#claim. The title ("AI Event Import: Turn a Flyer into an Event") and meta lead with the free parse instead of Enterprise generation, and the translated descriptions no longer say 11 languages. |
| ✅ | ✅ | Calendar Sync | `/features/calendar-sync` | "The Round Trip" ground-up rebuild. 2026-09 pass: reviewed against `GoogleCalendarService`, the Graph sync and the CalDAV poller - both legs, the fifteen-minute sweep, the delete-sync choice and the loop guard are all stated correctly; no changes needed. PASS 2: FAQ set re-read against GoogleCalendarService and the CalDAV poller and found exact, including the fifteen-minute backstop and the three delete policies; a11y sweep applied. ROUND 3: The audience band now presents the live iCal feed as the guest-side counterpart to sync (the last row of the Add to Calendar menu and the sign-up panel link), with an honest line on the one-hour cache and Google's refresh lag and a new FAQ separating sync from the feed. The delete copy now says completed boosts protect an event too and that an event another schedule owns is only taken off yours. Title and meta name Outlook instead of Apple, which is not a sync provider. |
| ✅ | ✅ | Analytics | `/features/analytics` | "The Dashboard" ground-up rebuild. 2026-09 pass: the intake gates, the nine dials, the one-row-a-day record and the revenue/check-in tabs all re-verified and already exact (this page had the QR-scanning tier right when three others had it wrong); the "want names" line now states the automatic subscriber digest and that it sits outside the newsletter allowance. PASS 2: its counted claim is now pinned by MarketingCountedClaimsTest, which asserts the number against the code it counts and names the source in the failure message. (110 crawler signatures, against `PageView::isBot()`.) ROUND 3: Link clicks were described as social links "split by platform"; since every link on a schedule has a short address and an unrecognised site answers to its own name, the dial, the Where they went next card, the JSON-LD and a new FAQ say so, with the per-link totals and the change warning. Title and meta rewritten around "no third-party analytics and no visitor log"; the newsletter footnote counts recipients. |
| ✅ | ✅ | Integrations | `/features/integrations` | "The Wire" ground-up rebuild. 2026-09 pass: Payfast was a shipped gateway missing from a page that claims to list every port in full, so the panel and register are now thirteen ports with its ZAR-only constraint, ITN callback and cart/installment exclusions written down; the fourteen webhook types re-verified against `Webhook::EVENT_TYPES`. PASS 2: its counted claim is now pinned by MarketingCountedClaimsTest, which asserts the number against the code it counts and names the source in the failure message. (Fourteen webhook event types, against `Webhook::EVENT_TYPES`.) ROUND 3: The page claims to list every port but left out Google Wallet (shipped 09-07); it is now port 15 on the panel, the register and an operator card, written as a buyer convenience, with every count moved to fifteen. The section-card port numbers had drifted two behind the panel since Payfast and PayPal were added; renumbered, with PayPal ahead of Payfast. Each money port says what a refund does, plus a refund FAQ; the fourteen webhook types re-verified (refunds added none). |
| ✅ | ✅ | Custom Fields | `/features/custom-fields` | "The Form" ground-up rebuild. 2026-09 pass: the ten-field cap, the six types, the four switches and the five validation presets all re-verified against `RoleUpdateRequest` and the edit form; the free/Pro line on this page was already correct about scanning and the 25-ticket allowance. PASS 2: same grid trap as newsletters - the sales-CSV extract table forced a 552px track in a 390px viewport; fixed with min-w-0 on the item. PASS 2: reviewed; the six types and the ten-field cap re-verified against `RoleUpdateRequest`, and the grid trap that scrolled the page at 390px fixed. ROUND 3: The "Three places" section called all three field homes Pro, but ticket-type fields have no plan gate in the edit form, EventRepo or checkout, so that home is tagged Free (schedule and event fields and per-guest answers stay Pro; the owner should confirm the ungated half is intended). The import card's "10 a day on Free" was wrong (10 is the trial allowance; a free schedule gets 50), so per-plan numbers were cut; PayPal is named beside Stripe; a per-guest answers FAQ added. |
| ✅ | ✅ | Custom Labels | `/features/custom-labels` | "The Rename" ground-up rebuild. 2026-09 pass: reviewed, accurate as written - the override set is per schedule rather than per account, and the page is careful that renaming a word moves nothing else on the page. PASS 2: the specimen table claimed to print every renameable label and was missing one - "Email me new events", added with the audience feature - so the page advertised 34 while the app offered 35. Added, all five counts corrected, and MarketingCustomLabelsTest now asserts the table against `Role::getCustomizableLabels()`. ROUND 3: The label set is unchanged at 36, so the table and every count stand. FAQ 1 said renaming Events changes "every place that word appears", which is false (Filter Events, Past Events and others are labels of their own, and the Add to Calendar menu lines are fixed text); corrected, with two new FAQs. Two examples that promised email the product does not send moved onto the Keep me posted button; new title. |
| ✅ | ✅ | Team Scheduling | `/features/team-scheduling` | "The Lineup" ground-up rebuild. 2026-09 pass: the owner card claimed the position "cannot be handed over from the Team tab" when Transfer ownership is a button on that exact tab, deliberately un-gated by plan - corrected, since it is the one multi-person action a free schedule has. PASS 2: re-read against the Team tab and the three access levels; accurate, including that a viewer can still scan at the door. A11y sweep applied. ROUND 3: The page predated team admins seeing the schedule's ticket sales, waitlist and check-in dashboard and refunding a sale, with viewers kept off sales: now in two new matrix rows (twelve in all), the position cards, the access FAQ, the JSON-LD and the meta. The matrix footnote wrongly put billing and delete on the Team tab; corrected. The alert list gained its installments toggle, and a transfer FAQ and a line on listed acts claiming their own page were added. |
| ✅ | ✅ | Sub-schedules | `/features/sub-schedules` | "The Sort" ground-up rebuild. 2026-09 pass: reviewed, accurate as written - the four-field record matches the `Group` model exactly, and the page is careful that a sub-schedule sorts rather than hides. PASS 2: re-read; its central claim (a sub-schedule sorts and never hides) is exactly what the model stores. A11y sweep applied. ROUND 3: Accurate as it stood; additions only: the embed can open on a single sub-schedule, curator event sources can file into a chosen sub-schedule as events arrive, and the live calendar feed covers the whole schedule (new FAQ). Links to /features/passes and the #customize-subschedules docs anchor; title and meta say "event calendar" instead of the drawer metaphor. |
| ✅ | ✅ | Online Events | `/features/online-events` | "Go Live" ground-up rebuild. 2026-09 pass: reviewed and accurate throughout, including the three attendance modes in the structured data, the domain-on-the-listing / full-link-on-the-ticket split, the Teams exception, and the schedule-timezone rule with its honest caveat; no changes needed. PASS 2: re-read; the join-link visibility and the .ics location answer both match the code. A11y sweep applied. ROUND 3: The tickets and paid-plan FAQs said Pro adds QR check-in, but scanning is free; both now list per-attendee tickets and the ticket waitlist. Payment reads Stripe or PayPal; a new FAQ on a moved or cancelled session covers the interest list, the SMTP condition for buyers and refunds; the live feed joins the timezone card; the ticket always shows the current join link. New title "Online & Hybrid Event Hosting, Any Platform". |
| ✅ | ✅ | Newsletters | `/features/newsletters` | "The Send" ground-up rebuild. 2026-09 pass: the page was built on "nothing sends itself", which `app:send-event-announcements` made false. Section 01 is now the two real rails - an automatic, batched, three-day-floored digest to confirmed subscribers that spends no envelopes, beside the newsletter you write - with the hero, dot nav and the style block`s concept note rewritten to match and `.es-send-void` removed as dead. PASS 2: the allowance table sat in a grid track sized to its min-content, so the page scrolled sideways at 390px; the item now carries min-w-0. ROUND 3: The interest list made "exactly one thing leaves without you" false; section 01 now separates three lists (the subscriber digest, the newsletter you write, and the email-only interest list per event, whose automatic mail is the on-sale email and a reminder). Also corrected: the segment list includes Email subscribers, All followers means people who pressed Follow, the Waitlist segment lost a Pro badge the code does not enforce, and the send gate applies only above 50 recipients. A confirmed sign-up now gets an account; new FAQ on the three lists; new title and meta. |
| ✅ | ✅ | Recurring Events | `/features/recurring-events` | "The Loop" ground-up rebuild. 2026-09 pass: the six patterns, three end conditions and both exception directions re-verified against `Event::matchesDate()`; the page title and JSON-LD said "automatic Google Calendar sync" when Outlook and CalDAV sync the same way, now corrected. PASS 2: reviewed; the six patterns and three end conditions re-verified against `Event::matchesDate()`. ROUND 3: Added the two ways guests now keep up with a series: the live calendar feed and the per-date interest list (two FAQs, a ledger row and JSON-LD). Corrected "Nothing is sent on your behalf" (confirmed subscribers get the 72-hour digest), check-in sold as a Pro add-on (scanning is free) and the Pro-only waitlist card (the RSVP waitlist is free). Title now "Weekly, Monthly & Yearly"; links to passes, the waitlist, newsletters and the embed. |
| ✅ | ✅ | Embed Calendar | `/features/embed-calendar` | "The Paste" ground-up rebuild. 2026-09 pass: reviewed and accurate throughout, including all six URL parameters, the noindex on the embed URL, the deliberate exclusion of embed loads from view counts, and the live iframe demo on the page itself; no changes needed. PASS 2: meta description rewritten from a feature list into the three things a reader is actually deciding - one tag, follows the visitor's dark mode, never needs pasting twice. The twelve-language claim is pinned by MarketingCountedClaimsTest. ROUND 3: Traced the embed code path: the frame is the calendar only, with no Add to Calendar menu, no interest box and no sign-up panel or feed link. A new row and FAQ say so and point to the event page a click opens in a new tab and to the feed URL in settings. The twelve-language claim and the pass-2 meta description are untouched. |
| ✅ | ✅ | Embed Tickets | `/features/embed-tickets` | "The Widget" ground-up rebuild. 2026-09 pass: reviewed and accurate - the seven layers are the real checkout, the sold-out state becomes the waitlist form, and the free RSVP variant is correctly separated from the Pro ticket widget; no changes needed. PASS 2: re-read against the widget's own gating and the recurrence question; accurate. A11y sweep applied. ROUND 3: The payment section was stale after PayPal: every method except cash leaves the frame, and Stripe, PayPal and Payfast send the buyer to their ticket page. The Pro card no longer lists the free RSVP form and free scanning under a Pro badge, and the JSON-LD offer no longer prices a Pro feature at 0. Added an FAQ on what stays on the full event page (the cart, the interest link, the checkout opt-in), the monthly option, a Google Wallet clause and PayPal and promo-code link cards; new title and H1. |
| ✅ | ✅ | Fan Videos | `/features/fan-videos` | "The Reel" ground-up rebuild. 2026-09 pass: reviewed and accurate - photos, YouTube links and comments, the reject-deletes-rather-than-hides rule, the per-occurrence filing on a recurring show, and the free-tier 25-photo cap all check out; no changes needed. PASS 2: it still said nothing is emailed to a follower automatically, full stop. Rewritten to name the other list - the people who left an email address and confirmed it - which is the one that hears on its own. ROUND 3: Re-checked the 25-photo free cap, the approval queue and per-date filing; the whole-lineup change does not affect attribution, because fan content files against agenda parts. The title leads with photos, Where it plays adds the list-view cards (up to three videos, four photos and two comments on wider screens), and the followers FAQ names the 72-hour digest. |
| ✅ | ✅ | Polls | `/features/polls` | "The Vote" ground-up rebuild. 2026-09 pass: reviewed against `EventPoll` - the five-per-event and two-to-ten limits, the seal-on-first-vote rule, and both write-in modes including the pending queue are all stated correctly; no changes needed. PASS 2: reviewed; the five-per-event and two-to-ten-choice limits re-verified against `EventPoll`. ROUND 3: The event-page card implied Unlisted link holders could vote; votePoll limits voting on Draft, Internal and Unlisted events to schedule members, and the card and FAQ now say so. Added "Can I see who voted for what?" (counts and shares, not names) and the confirmed-subscriber digest beside the Follow line. Title "Event Polls: Let Your Audience Vote on the Event Page"; limits re-pinned against EventPoll. |
| ✅ | ✅ | Boost | `/features/boost` | "The Launch" ground-up rebuild. 2026-09 pass: reviewed and accurate - five pre-launch gates, both channels (Meta and the on-network promotion), prepay-and-refund, and the audience section already states the automatic subscriber digest correctly; no changes needed. PASS 2: re-read; the Pro gate, the phone-verification requirement and the prepaid budget are all as the code has them, and the page already carries its own Keep exploring rail. A11y sweep applied. ROUND 3: Re-verified the Pro gate, the phone check, the budget table and the network-promotion gates. "Real-time analytics" replaced (stats sync every 15 minutes), the digest floor pinned at 72 hours, and the landing-page card and a new FAQ name the free interest list for a boost that runs before tickets are on sale. Title leads with "Promote Events with Facebook and Instagram Ads". |
| ✅ | ✅ | Private Events | `/features/private-events` | "The Vault" ground-up rebuild. 2026-09 pass: reviewed and accurate - the four states match `Event::visibilityState()` exactly, with the password correctly attached to Unlisted and both Enterprise states badged; no changes needed. PASS 2: reviewed; the four visibility states re-verified against `Event::visibilityState()`. ROUND 3: The four states re-verified against Event::visibilityState(); added what an Unlisted reader asks about: the live feed and the automatic digest take public events only, and the interest sign-up is never offered on a hidden event and is re-checked before each email (a cancellation notice still goes out). New FAQs "Who can see an unlisted event?" and "What if people asked to hear about an event I then hide?"; the title names members-only, unlisted and password-protected events. |
| ✅ | ✅ | Event Graphics | `/features/event-graphics` | "The Gallery" ground-up rebuild. 2026-09 pass: reviewed and accurate - the generator hangs flyers already on the events rather than pretending to be a design tool, and the template-variable list matches `EventTextGenerator::parseTemplate()`; no changes needed. PASS 2: meta description rewritten in the page's own voice with what actually happens (pick a size and a template, the values fill themselves in). Template-variable list re-checked against EventTextGenerator::parseTemplate and against its twin on /docs/creating-schedules - 39 variables, no drift either way. ROUND 3: The template variables (39) and image sizes are unchanged since 09-04 and still match both docs twin lists. The H1 carries the topic ("Event graphics from your flyers."), the title reads "Social Posts From Flyers", a new FAQ answers whether graphics carry our branding (every hosted graphic carries an eventschedule.com credit on any plan), and the QR card links the interest list. |
| ✅ | ✅ | White Label | `/features/white-label` | "The Blank Slate" ground-up rebuild. 2026-09 pass: reviewed against `Role::showBranding()` / `creditChipReason()` and BRANDING_MATRIX.md - all seven surfaces, the plan-tier keying and the credit chip that no plan buys off are stated exactly; no changes needed. PASS 2: the seven-surface claim re-counted against the page`s own table (7 rows plus a header) and the branding matrix; accents raised to AA. ROUND 3: Step 01 printed a raw PHP echo instead of the Pro price; fixed. "Every place our name lands" was false twice over: every hosted event graphic is stamped eventschedule.com, and a Google Wallet pass falls back to our logo when a schedule has no profile image; section 03 now has four cards (a full 2x2) and "Is anything left?" names both. Unclaimed act pages carry the strip as free schedules and interest emails carry no "Powered by", so the count stays at seven. Title, meta, eyebrow and H1 now carry "white label". |
| ✅ | ✅ | Custom CSS | `/features/custom-css` | "The Stylesheet" ground-up rebuild. 2026-09 pass: reviewed and accurate - the cascade position (same sheet, immediately after the generated styles, equal specificity) is the honest description of what the feature is; no changes needed. PASS 2: the three 'what it pairs with' links measured 19px tall, under the WCAG 2.2 target minimum; vertical padding on the inline box lifts them clear without moving the row. Decorative glyphs marked aria-hidden. ROUND 3: "One sheet. Every guest page." overclaimed: only the guest layout emits custom CSS, and the ticket, order and installment pages a buyer reaches after paying use the app layout. Section 05 now reads "Every page in the guest layout", with a new FAQ listing the pages it reaches. The On Pro card listed "ticketing with QR check-in", which is free; it now says unlimited paid tickets, and the Free card says 25 a month. The meta states the cascade fact and the 10,000-character limit. |
| ✅ | ✅ | Custom Domain | `/features/custom-domain` | "The Nameplate" ground-up rebuild. 2026-09 pass: reviewed and accurate - both modes, automatic HTTPS once the CNAME resolves, and the rewriting of links, feeds and the checkout return URL all check out against `ResolveCustomDomain` and the provisioning contract; no changes needed. PASS 2: meta description replaced its "professional branded URL" filler with the two facts that decide it - HTTPS is issued once the CNAME resolves, and the links you send out carry your domain rather than ours. ROUND 3: The page now says, with sources, that newsletter, announcement and interest-list links use the custom domain, as do the subscribable feed and every short link (yourdomain.com/instagram, clicks counted); checkout copy names PayPal, whose return URL also comes back to the domain. The meta was narrowed to what is verified: event links in emails and feeds carry your domain, while system links such as ticket pages and unsubscribe do not. |
| ✅ | ✅ | Feedback | `/features/feedback` | "The Comment Card" ground-up rebuild. 2026-09 pass: reviewed and accurate - one card per booking, the 1-to-48-hour window, the 30-day cutoff, the 2,000-character comment cap and the `feedback.submitted` webhook all check out; no changes needed. PASS 2: re-read against FeedbackController; the timing, the per-booking card count and the moderation answer are all exact. A11y sweep applied. ROUND 3: The H1 read "per ticket" while the page promises one card per booking; it now reads "A feedback card per booking", and the title is "Post-Event Feedback and Attendee Reviews". Added the refund story from the code: a booking refunded in full is not asked and its link stops opening, a card it already returned leaves the public reviews, averages and API but stays in the tab list and the CSV, a partial refund changes nothing, and a multi-event checkout gets one card per event. |
| ✅ | ✅ | Availability | `/features/availability` | "Office Hours" ground-up rebuild. 2026-09 pass: reviewed and accurate - the cross-out-what-is-gone model, the Enterprise talent-schedule scope, and its separation from bookable appointments are all correct; no changes needed. PASS 2: re-read; the talent-schedule scope, the per-member dates and the privacy answer match the code. A11y sweep applied. ROUND 3: The comparison table and the Bookable time tile still said appointments were Pro; one type is free on any schedule and Pro removes the limit, with a new FAQ on availability versus appointment booking. "Who can set availability" was tightened (a viewer cannot mark dates), the live feed is named where the page promises nothing is published, and the title carries "Availability Calendar". |
| ✅ | ✅ | Appointments | `/features/appointments` | "The Appointment Book" ground-up rebuild. 2026-09 pass: the page still called booking a Pro feature in four places. `Role::appointmentTypeLimit()` gives a free hosted schedule one fully-featured type and Pro uncaps the count, so the FAQ, the plan note, the availability comparison and the finale line now say that, including what a lapse actually does (every type kept, the oldest bookable one stays bookable). PASS 2: plan band added - the page argues appointments are free and one type is the free ceiling, and now shows what lifting it costs. Decorative glyphs marked aria-hidden. ROUND 3: The 09-08 refund edits told owners to cancel and then refund on the Sales page, which cannot work for a Stripe booking (Refund only appears while the sale is paid); both refund cards and a new FAQ now say refund instead of cancelling, in full or in part, with payment-link and cash bookings marked refunded. Added that PayPal, Payfast and Invoice Ninja cannot take an appointment payment, that bookings never count against the 25 paid tickets, and FAQs for booking-tool shoppers with a link to /calendly-replacement. Google Wallet is deliberately absent, because bookings get no pass. |
| ✅ | ✅ | Carpool | `/features/carpool` | "Four Seats" ground-up rebuild. 2026-09 pass: reviewed and accurate - the six-field offer record, driver approval before contact details are shared, and the deliberate absence of routing or payment are all stated plainly; no changes needed. PASS 2: meta description rewritten around the thing readers actually ask about, which is who sees a phone number and when - the driver approves each rider before any contact detail is shared. ROUND 3: Still accurate; an SEO and clarity pass. The title now carries the page's spine (the driver approves every rider), the meta is tightened with "email or phone number", and two buyer FAQs were added: no ticket or RSVP is needed to join, and a ride offer has no price, so fuel money never passes through the app. |
| ✅ | ✅ | Google Calendar | `/google-calendar` | "The Invitation" ground-up rebuild; root-level integration page. 2026-09 pass: reviewed and accurate - OAuth connect, per-schedule calendar choice, the watch channel plus fifteen-minute sweep, and the location-to-venue conversion all check out; its Google-only framing is correct for a page about Google. PASS 2: two questions the page could not answer, both now grounded in the controller - connecting does NOT backfill events already on the schedule (forceSyncToGoogle, owner-only and push-only, is what does), and disconnecting deliberately leaves what it already wrote in Google alone while closing the channels and clearing every schedule's direction. Decorative glyphs marked aria-hidden. ROUND 3: The H1, title and meta now plainly name Google Calendar (the meta leaned on "smooth"), and the meta states the within-seconds inbound read GoogleCalendarWebhookController does. The guest side became a card and FAQ on the live feed ("Subscribe to all events from your schedule"), kept apart from sync. The delete policy states its brake (an event with ticket sales is hidden, not deleted), and the channel renewal is described as the daily check it is. |
| ✅ | ✅ | Outlook Calendar | `/outlook-calendar` | "The Meeting Request" ground-up rebuild; root-level integration page. 2026-09 pass: reviewed and accurate - Graph change subscriptions, delta tokens, the optional Teams meeting written back into the event link field, and the schedule-timezone stamping all check out; no changes needed. PASS 2: the page stated its Free and Pro chips but had no plan band and no link to /pricing at all, so a reader who reached the bottom had to go back to the header; the shared band now closes it. Decorative glyphs marked aria-hidden. ROUND 3: Two wrong claims fixed: the delete policy is shared with Google only (CalDAV has none), and inbound sync is "usually within a minute or two", not "near real time", because Graph notifications are debounced and queued. The feed section, a new FAQ and the JSON-LD cover the guest feed, the H1 names Outlook, and the title is "Free Two-Way Outlook & Microsoft 365 Calendar Sync". |
| ✅ | ✅ | CalDAV | `/caldav` | "The Open Protocol" ground-up rebuild; root-level integration page. 2026-09 pass: reviewed and accurate - RFC 4791 over HTTPS only, encrypted credentials, the six requests, and the honest fifteen-minute poll because CalDAV has no notification standard; no changes needed. PASS 2: same missing plan band as its two sibling calendar pages, now carrying the shared three-card band before Related pages. Decorative glyphs marked aria-hidden. ROUND 3: Re-verified the fifteen-minute poll with its ctag pre-check and that CalDAV applies no deletion policy, so its delete FAQ stands. The title (37 characters before) now names iCloud, Nextcloud and Fastmail, the meta was rewritten and the H1 names CalDAV. A new FAQ and the audience line point guests at the read-only live feed; LOCATION matching says "the venues you already have". |
| ✅ | ✅ | Stripe | `/stripe` | "The Payout" ground-up rebuild; root-level integration page. 2026-09 pass: reviewed and accurate - the charge is created on the connected account with no application fee, and the page argues zero fees as a statement line rather than a promise; no changes needed. PASS 2: re-read end to end against the Connect flow; the payout answer, the currency answer and the abandoned-checkout answer are exact, and the \/bin/zsh.30 and 2.9% figures are Stripe's own USD and correctly left hardcoded. A11y sweep applied. ROUND 3: Refunds were half right and installments, the one Stripe-only feature, were missing. The refund FAQ matches SaleRefundService (money back first, full or partial, plans leg by leg), and a new FAQ replaces the advice to refund in the Stripe Dashboard: no refund event comes back from Stripe, so that sale would still read paid and its tickets would still scan. Installments gained a Same rail card, an FAQ, a JSON-LD item and a link; the route grid went to three columns so its last row is full; the API is no longer called read-only. |
| ✅ | ✅ | PayPal | `/paypal` | "The Last Check" ground-up rebuild, 2026-09-09; root-level integration page. Shipped `6e2b6bc1a` onto six list-type pages with no page of its own while Stripe and Invoice Ninja each had one. The concept is the verification, which is what `PayPalGateway` genuinely does differently: credentials minted against PayPal before the settings form will save them, the sale re-checked BEFORE the capture (the capture is the money here, unlike every sibling driver), and the result read back rather than trusted from a webhook. A first draft argued "more ways to pay at checkout" and was cut - `Event.payment_method` is a single select, so that would have described a product we do not ship. Free on every tier: there is no `isPro()` anywhere in `app/Services/Payments/`. ROUND 3: Updated for the 09-08 to 09-10 capture and refund work: a new FAQ on a payment PayPal holds for review (the seats stay held, not resold, and the ticket is emailed when the capture completes); refunds from the Sales page (a sale marked paid by hand falls back to Mark as Refunded, and dashboard refunds are not reported back); gift cards and appointments named beside installments as exclusions; rand pointed at Payfast. Title carries a searcher phrase; the guide button opens /docs/tickets#paypal. |
| ✅ | ✅ | Invoice Ninja | `/invoiceninja` | "The Ledger" ground-up rebuild; root-level integration page. 2026-09 pass: verified the tier this page implies - Invoice Ninja has NO `isPro()` gate anywhere (gateway, controller, or the settings write), so `docs/FEATURES.md` was corrected to move it out of the Pro table, and `/about` no longer calls it a Pro-only payment route. PASS 2: plan band added (the page named plan tiers but never priced them). Decorative glyphs marked aria-hidden. ROUND 3: The page never mentioned refunds: a new FAQ says an Invoice Ninja sale can only be marked refunded (the seats return, no money moves, nothing changes in Invoice Ninja). A second FAQ says one invoice cannot span several events, because the cart runs on Stripe, PayPal and cash only. The payment-method list now includes PayPal and Payfast; the rest was accurate and is unchanged. |

## Schedule Type Hubs (3)

> The three top-level audience pages, one per schedule type (Talent, Venue, Curator). Each lives at `resources/views/marketing/for-{slug}.blade.php`.

| Reviewed | Updated | Page | URL | Notes |
|:--------:|:-------:|------|-----|-------|
| ✅ | ✅ | For Talent | `/for-talent` | Rebuilt July 2026 onto the shared components off `config/marketing_audiences.php`. 2026-09 pass: the follower story is now the automatic subscriber digest plus the newsletter you write, in the "fill the room" banner, the free-plan strip and the FAQ; fee comparison and plan split re-verified. PASS 2: contrast clean; muted text raised where the page`s tinted ground put gray-500 under 4.5. ROUND 3: The ticket banner, free-plan strip and JSON-LD read as Stripe-only; they now name Stripe or PayPal, cash at the door and refunds from the Sales page. The claim flow is answered in the Get booked banner and a new FAQ ("A venue or curator made a page for me. How do I claim it?": who created it, per-date credit, out of search until claimed, claimed with the address on it, existing listers kept, This is not me). The fans FAQ and the Fill the room banner gained the live feed and the interest list, schedule graphics are marked free, and the title gained "Gig Calendar". |
| ✅ | ✅ | For Venues | `/for-venues` | "Front of House" ground-up rebuild. 2026-09 pass: reviewed and accurate throughout - the 25-ticket allowance, un-gated door scanning, the one-free/five-Enterprise team split and the free bookable appointment type are all stated correctly; removed a dead `.es-od-strip` rule left behind by an earlier edit. PASS 2: contrast clean; both twelve-colour card grids unified on one link colour so the icon chips carry identity; the "Accepted" stamp ink, the room counts and the comparison header all raised to AA. ROUND 3: The box-office banner, cost FAQ and plans lede said Stripe only and now say Stripe or PayPal or cash, with refunds from the Sales page. Three FAQs added: refunds, the interest list (the on-sale email, cancellation notice and reminder are automatic, a change notice only when sent) and booking an act who is not on Event Schedule (whole lineup, claimable page, invitation, dates kept after a claim); private hire corrected to Stripe, a payment link or cash. The door banner says scanning is free and the dashboard Pro, team admins see ticket sales, allocated seating joined the Enterprise card, and the title names calendar, bookings and tickets. |
| ✅ | ✅ | For Curators | `/for-curators` | "The Listings" ground-up rebuild. 2026-09 pass: reviewed and accurate; fixed a heading-level skip (h2 straight to h4) in the "and so" trio, which is the only a11y defect the whole for-page set turned up. PASS 2: contrast clean; muted copy raised to gray-600 on the page`s cream ground. ROUND 3: Event sources were missing entirely: the Three ways FAQ became four, and the desk copy, press-run step and listings column now lead with following talent and venue schedules. The listings gained a row for the pages named acts get (with a new FAQ on the claimable page, the invitation and the approved list), a signed-in submitter hears by email on accept and decline, the discovery FAQ adds the live feed, and schedule graphics are marked free. Title and description drop "ultimate" for a local events guide that fills itself. |

## Audience "For" pages (31)

> **Restyle briefs.** Each note below can be pasted into Claude as the instruction for making that page's design more unique to its audience. Each page lives at `resources/views/marketing/for-{slug}.blade.php`. On most pages the existing signature is the accent gradient on the seven section headings plus a motif rendered in exactly three full-bleed layers (hero art, dark band, finale); the briefs below extend it deeper into the page. Shared ground rules for every brief: keep the shared es-* skeleton and section order; implement theming inside the page's nonce'd `<style>` block following the `/* For-x "Nickname" styles */` convention; every gradient needs light and dark variants; every animation needs a reduced-motion kill-switch; carry the accent and motif into at least one mid-page moment (bento, stats or week grid) and recolor the hard-coded blue "See all features" link and related-card hovers to the page accent; remove or actually use any dead `es-*-float` class; never use purple, violet, indigo, fuchsia or pink as accents; never use decorative line drawings (outline SVG illustrations of objects or scenes) - see CLAUDE.md; no new dependencies or external assets.

> **Review status of the last 14 (2026-07-30).** The final 14 pages were rebuilt in one
> parallel pass. Each was verified MECHANICALLY: HTTP 200, `audit-blade.py` reporting 0
> problems (dead classes, undefined page-local classes, orphan descendant rules, forbidden
> colours, em-dashes, reduced-motion kill-switches), and 0 WCAG AA contrast failures in both
> colour modes. They did NOT receive the independent adversarial design/fabrication review
> that the earlier 17 got, because the reviewing pass was cut short by a usage limit. Their
> authors each reported their own accuracy sweep (fabricated features removed, plan tiers
> corrected), but that self-report is unaudited. Worth a human read for design substance and
> feature-claim accuracy before treating them as final. **Every OTHER page rebuilt in this
> campaign (main, features, templates, legal) did get its independent reviewer.**

| Reviewed | Updated | Page | URL | Notes |
|:--------:|:-------:|------|-----|-------|
| ✅ | ✅ | For Musicians | `/for-musicians` | "The Tour Poster" ground-up rebuild. 2026-09 pass: reviewed; calendar-sync card now names Outlook and CalDAV, and the shared plan band it renders was rebuilt (it billed QR check-in as Pro). PASS 2: contrast clean; the poster-stamp cyan and the accent link darkened for this page`s warm poster stock. PASS 2: benefits from the shared Pass 2 work - zero WCAG AA contrast failures in both colour modes, no horizontal scroll at 390px, and the "Keep exploring" block now carries the site`s standard lift and brand hairline. Page-specific: two claims that fans "get notified about shows near them" removed (no proximity notifier exists) and the discovery FAQ rewritten around the real subscriber digest. ROUND 3: Three stale claims corrected wherever they appeared: "a venue books you and it lands on your poster automatically" now describes the request an act with an account accepts; the last proximity claims ("nearby") and "followers get your dates the moment you post them" now describe the batched subscriber digest; and show art is on every plan, not Pro. Added claiming a page a venue created before you joined, the live feed, the ticket interest list, PayPal and cash beside Stripe, refunds and short link addresses with click counts; team logins marked Enterprise, promo codes and waitlists Pro; two new FAQs and a meta description built around tour dates and venue bills. |
| ✅ | ✅ | For DJs | `/for-djs` | "The Neon Sign" ground-up rebuild. 2026-09 pass: reviewed; calendar-sync card corrected to name all three carriers, shared plan band rebuilt. PASS 2: contrast clean; the "Platform fee" eyebrow raised to emerald-700. ROUND 3: Corrected "a promoter adds you and it appears automatically" (an act with an account accepts a request), marked manager or agency access Enterprise, and turned "Templates clone it" (templates are Pro) into free event cloning. The Stripe-only ticket FAQ and mock now name PayPal and cash; the live feed joined the share-link card and the interest list and refunds the ticket FAQ; two new FAQs (claiming a page a promoter created, subscribing to set times); "nearby-show alerts" in the featureList became the real digest. |
| ✅ | ✅ | For Comedians | `/for-comedians` | "The Tight Five" ground-up rebuild. 2026-09 pass: reviewed; calendar-sync card corrected, shared plan band rebuilt. PASS 2: contrast clean; body copy raised to gray-600 on this page`s warm ground. ROUND 3: Removed an invented feature (no weekly stage-time total exists), an impossible newsletter mock ("Sent to 1,247 fans" exceeds even the Enterprise allowance; now 96), a finale "No premium tier" that contradicted the plan band, and "Never double-book a mic night", which contradicted the /about errata; polls and feedback are marked Pro. Bit 03 and the clubs FAQ describe request-and-accept plus the page a club's listing creates for you to claim; payments name PayPal and cash with refunds; new FAQs on how fans hear about new shows and on ticket alerts. |
| ✅ | ✅ | For Circus and Acrobatics | `/for-circus-acrobatics` | "The Center Ring" ground-up rebuild. 2026-09 pass: the discovery FAQ said there is no automatic alert, which `app:send-event-announcements` made false - now the subscriber digest plus the newsletter you write; calendar-sync card corrected. PASS 2: its ticketing answer was the only one in the family that did not state the free ceiling, so a reader comparing two audience pages got two different pictures. Now says 25 paid tickets a month free, Pro lifts it. ROUND 3: Act V and the ticket FAQ read as Stripe-only and now name PayPal, Payfast, Invoice Ninja, a payment link and cash, with Stripe or PayPal refunds for a rained-off show; the FAQ's offer of an Enterprise seat map to a troupe was corrected (seat maps are drawn on venue schedules), and show posters now say every plan. Added the live feed, the interest list, a claim-your-page FAQ for acts a festival already listed, and the digest of dates you add; the title ends "Tours, Tickets" and the passes link goes to /features/passes. |
| ✅ | ✅ | For Magicians | `/for-magicians` | "Pick a Card" ground-up rebuild. 2026-09 pass: reviewed; calendar-sync card corrected, shared plan band rebuilt. PASS 2: contrast clean; the lede raised to gray-600 on the page`s card stock. ROUND 3: Removed four false availability claims (planners never see a member's availability, which is admin-only), replacing them with the Request to Book form talent schedules already have, and rewrote "you never double-book a Saturday", which contradicted the /about clash erratum. Ticketing names PayPal and the other methods plus refunds; the waitlist, passes, gift cards, templates, custom fields and ticket embed now say Pro; the discovery FAQ gained the live feed and the interest list, plus a claim-your-page FAQ; "effortless" cut. Coordinator (hosted email pass): the gift card FAQ says selling them needs the schedule's own email settings on eventschedule.com (Role::canSellGiftCards()). |
| ✅ | ✅ | For Spoken Word | `/for-spoken-word` | "The Sign-Up Sheet" ground-up rebuild. 2026-09 pass: reviewed and accurate - free registration with a per-date capacity, recurring dates with exceptions and the custom questions tier are all stated correctly. PASS 2: carried the stale "selling is Pro" claim in its own wording, which the Pass 1 sentence scan did not match; corrected against Role::ticketSaleLimit() (25 paid tickets a month free, scanning never gated) and now pinned by MarketingTicketingTierTest. ROUND 3: Two stale tier tags fixed: the ticketed-feature card was tagged Pro (now Free, naming Stripe, PayPal and cash and the 25 paid tickets a month, with promo codes and the season pass on the Pro line), and the flyer card and FAQ called graphics Pro. The pattern-check claim moved from the sign-up form to the event request form, where patterns actually exist; the sign-up FAQ gained the per-date count, self-cancel and the free registration waitlist; added the live feed, the digest, and FAQs on the interest list (plain registration sends no opening email) and on claiming a page a bookstore created. |
| ✅ | ✅ | For Bars | `/for-bars` | "The Chalkboard" ground-up rebuild. 2026-09 pass: the free/Pro answer put ticketing on the Pro side; selling is free to 25 paid tickets a month with door scanning included, so the answer now says that and Pro is the ceiling plus the live dashboard. PASS 2: carried the stale "selling is Pro" claim in its own wording, which the Pass 1 sentence scan did not match; corrected against Role::ticketSaleLimit() (25 paid tickets a month free, scanning never gated) and now pinned by MarketingTicketingTierTest. ROUND 3: The ticketed-night card was still tagged Pro and said "Connect Stripe"; it is now Free (25 paid tickets a month, door scanning ungated), names Stripe, PayPal, a payment link or cash, and flags promo codes and passes as the Pro part; the poll is marked Pro. Added the live feed, the email sign-up's automatic round-up of new nights, refunds in the ticketing FAQ, and a note that bands not on Event Schedule get an unclaimed, noindexed page; event graphics dropped from the Pro list. |
| ✅ | ✅ | For Nightclubs | `/for-nightclubs` | "The Door" ground-up rebuild. 2026-09 pass: reviewed and accurate - free registration as a zero-price ticket, time-based pricing via sales windows, and door scanning on every plan. PASS 2: carried the stale "selling is Pro" claim in its own wording, which the Pass 1 sentence scan did not match; corrected against Role::ticketSaleLimit() (25 paid tickets a month free, scanning never gated) and now pinned by MarketingTicketingTierTest. ROUND 3: The door section said "Check-in and the dashboard are Pro", and two ticket cards were tagged Pro although sales windows and volume discounts have no plan gate: scanning is now free (only the live dashboard and per-attendee tickets are Pro), timed cover is Free, and the tables card became Tables and group rates (Free, with add-ons the Pro part). The Stripe-only line was replaced; FAQs on the interest list and refunds, a Google Wallet bullet at the door, and event graphics dropped from the Pro list. |
| ✅ | ✅ | For Music Venues | `/for-music-venues` | "The Running Order" ground-up rebuild. 2026-09 pass: reviewed and accurate - event parts for set times, per-part fan content, and the sub-schedule split for main and back room. PASS 2: carried the stale "selling is Pro" claim in its own wording, which the Pass 1 sentence scan did not match; corrected against Role::ticketSaleLimit() (25 paid tickets a month free, scanning never gated) and now pinned by MarketingTicketingTierTest. ROUND 3: The door cards tagged ticket tiers and QR scanning as Pro and the fees copy was Stripe-only: tiers (sales windows, group rates) and scanning are Free, with add-ons and the dashboard named as Pro, and payment reads Stripe or PayPal, a payment link or cash. A One link for the band too card and a new FAQ cover the whole lineup and the noindexed page an act gets until it claims; added the interest list, a refunds FAQ, the poll marked Pro, and event graphics dropped from the Pro list. |
| ✅ | ✅ | For Theaters | `/for-theaters` | "The Run" ground-up rebuild. 2026-09 pass: reviewed and accurate - the run as one recurring event with dark nights excluded, per-date inventory, season passes and the Enterprise seat map. PASS 2: "25 tickets a month" tightened to 25 PAID tickets, with the zero-price exemption named - the cap counts paid ones and a free RSVP never touches it, which for a theatre running previews is the difference that matters. ROUND 3: Scanning at the door was still billed Pro (the On the door badge, FAQ 1 and the finale), the payment line read Stripe-only, the group rate sat in a Pro card though it is ungated, and the graphics card read Pro; all corrected. Added the interest list to the Announce card, reserved-seating detail (best seats together, the box office's pick-several hold, phone booking, move and release, arrivals on the map), installments, the multi-event cart, Google Wallet at the door, two FAQs (interest list, refunds) and Key features rows for Allocated Seating and Passes. |
| ✅ | ✅ | For Dance Groups | `/for-dance-groups` | "The Barre" ground-up rebuild. 2026-09 pass: a feature row billed QR check-in as Pro; scanning has no plan check, so the row is now the live check-in view and says which half is Pro. PASS 2: said selling anything, class cards and show tickets alike, was Pro. Show tickets are free to the 25-a-month ceiling; the PASS is the Pro part, which is the distinction a studio actually cares about. ROUND 3: The show's Zero platform fees card was tagged Pro because the badge ignored each row's tier; it now follows the tier. The payment footnote read Stripe-only, and the hero said "Tickets from $22" while its own panel starts at $12. Added the interest list to the show lede, the live feed to the embed card, the class card marked Pro in its FAQ, two FAQs (recital tickets going on sale, the timetable in your own calendar) and a Passes row in Key features. |
| ✅ | ✅ | For Theater Performers | `/for-theater-performers` | "The Résumé" ground-up rebuild. 2026-09 pass: reviewed and accurate - the List layout as a self-maintaining credits list, and events arriving from the company that cast you. PASS 2: carried the stale "selling is Pro" claim in its own wording, which the Pass 1 sentence scan did not match; corrected against Role::ticketSaleLimit() (25 paid tickets a month free, scanning never gated) and now pinned by MarketingTicketingTierTest. ROUND 3: Corrected four false claims: "the ones you trust post straight to the page" (a talent schedule cannot set an approved list and always receives requests), "Nothing is public until you accept it" (a company's event page lists its cast either way), Producing cards billing ticket types, scanning and zero fees as Pro, and Stripe-only payment lines; an unbacked "a short run usually fits inside the free plan" became the real 48-hour rule. Added the claim flow in the page's voice: a full-width card under the cast grid, How it works step 03 and a new FAQ. |
| ✅ | ✅ | For Food Trucks and Vendors | `/for-food-trucks-and-vendors` | "Today's Stop" ground-up rebuild. 2026-09 pass: the "do my followers get an alert" answer was a flat no; subscribers now get a batched digest of new stops, with account followers still newsletter-only. PASS 2: carried the stale "selling is Pro" claim in its own wording, which the Pass 1 sentence scan did not match; corrected against Role::ticketSaleLimit() (25 paid tickets a month free, scanning never gated) and now pinned by MarketingTicketingTierTest. Its follower card also still said nothing goes out automatically, which app:send-event-announcements invalidated; rewritten as the three-step it actually is. ROUND 3: Already accurate, so small edits. Added the live calendar feed this reader needs most (a fifth sticker card, an FAQ, a featureList line and a docs link): regulars subscribe once, give no email, and see every new stop, moved stop or date exception in their own calendar; the meta description now leads with it. The interest list was considered and left out, because its label and first email are about tickets. |
| ✅ | ✅ | For Comedy Clubs | `/for-comedy-clubs` | "Friday at Eight" ground-up rebuild. 2026-09 pass: the free/Pro answer said selling tickets is on the Pro plan - it is free to 25 paid tickets a month with scanning included, and Pro is the ceiling plus the live dashboard. PASS 2: re-read against the code; its selling, waitlist and approved-list answers were already exact after the Pass 1 sweep. A11y sweep applied. ROUND 3: The selling section still badged ticket types, QR check-in and zero platform fees as Pro and read as Stripe-only; re-tiered against Role::ticketSaleLimit() and the ungated scanner (the live dashboard is the Pro part), with Stripe, PayPal, a payment link and cash named and a plan note on when a busy room passes the free 25. The claim copy now matches the public unclaimed page and the whole-lineup event page, and a comic with an account receives the date as a request. Added FAQs for the interest list and refunds, the free open-mic waitlist, and a meta description naming comics and zero platform fees. |
| ✅ | ✅ | For Restaurants | `/for-restaurants` | "Twenty-Four Covers" ground-up rebuild. 2026-09 pass: reviewed and accurate - RSVP capacity per date, sales windows that close before you shop, and custom questions for allergies. PASS 2: carried the stale "selling is Pro" claim in its own wording, which the Pass 1 sentence scan did not match; corrected against Role::ticketSaleLimit() (25 paid tickets a month free, scanning never gated) and now pinned by MarketingTicketingTierTest. Two sites, the FAQ and the band under the cutoff section. ROUND 3: Five stale Pro badges, and the finale still priced selling covers at the Pro rate; the code gates neither ticket-type questions nor sales windows, so the count, cutoff and questions panels are Free, with per-guest answers and the CSV export named as the Pro parts. Added a sales-open-on-a-date bullet tied to the interest list, FAQs for announcing a dinner before bookings open and for refunds, free per-date capacity in the free answer, and PayPal beside Stripe. |
| ✅ | ✅ | For Breweries and Wineries | `/for-breweries-and-wineries` | "Most Nights Are Free" ground-up rebuild. 2026-09 pass: the email answer said nothing goes out automatically; new dates now reach confirmed subscribers as a digest, with the allowance still counting the newsletters you write. PASS 2: carried the stale "selling is Pro" claim in its own wording, which the Pass 1 sentence scan did not match; corrected against Role::ticketSaleLimit() (25 paid tickets a month free, scanning never gated) and now pinned by MarketingTicketingTierTest. ROUND 3: Still sold the paid tour as Pro in four places MarketingTicketingTierTest does not match (a Pro plan pill, "the only thing on this page that needs Pro", a how-it-works step and the finale); they now say free to 25 paid tickets a month, with the arithmetic for when Pro starts to matter. Payments no longer read Stripe-only, and the participant copy no longer says a booked act's schedule takes the date automatically. Added the live feed, the interest list for a tour, refunds and claimable pages for bands, with two FAQs; the title leads with "Brewery and Winery Event Calendar". |
| ✅ | ✅ | For Art Galleries | `/for-art-galleries` | "Four Evenings" ground-up rebuild. 2026-09 pass: two places said there is no automatic announcement - a new show now reaches confirmed subscribers on its own, and the opening you want to write about is still yours. PASS 2: carried the stale "selling is Pro" claim in its own wording, which the Pass 1 sentence scan did not match; corrected against Role::ticketSaleLimit() (25 paid tickets a month free, scanning never gated) and now pinned by MarketingTicketingTierTest. ROUND 3: The collector-dinner card still carried a Pro plan pill, the proposals card said ten submissions a day free and fifty on Pro when free and Pro share the same 50 (10 on a trial, 100 on Enterprise), and the newsletter card's "two sends on Pro" arithmetic was impossible at 100 recipients a month. All corrected, "no automation" scoped to the newsletter, and dates no longer promised to land on an artist's schedule automatically. Added the live feed, the interest list, all payment methods with refunds, and a FAQ on exhibiting artists not on Event Schedule. |
| ✅ | ✅ | For Community Centers | `/for-community-centers` | "The Gathering Place" ground-up rebuild. 2026-09 pass: reviewed and accurate; shared plan band rebuilt. PASS 2: the week table`s sr-only day labels escaped its scroll box and widened the page at 390px; the wrapper is now a containing block. ROUND 3: The doors band still said followers are never emailed automatically and an FAQ called every door "yours to trigger", both false since the digest shipped; door 04 and the footnote now separate the digest (confirmed subscribers, the center's own events, at most one every 72 hours, can be switched off) from the newsletter, and the calendar door leads with the live feed. Paid classes no longer read Stripe-only, refunds and the interest list got FAQs, the Event Graphics card no longer says Pro, and six generic Who it is for descriptions were rewritten in the noticeboard voice. |
| ✅ | ✅ | For Fitness and Yoga | `/for-fitness-and-yoga` | "The Flow" ground-up rebuild. 2026-09 pass: the discovery answer said there is no automatic alert when you add a class; students who left an email now get one digest covering the batch. PASS 2: same sr-only escape as community-centers, fixed on `.es-flow-scroll`. ROUND 3: Four places read as if class payments were Stripe-only (the unit band, the pass FAQ, the pass-types footer and the featureList); all now name Stripe and PayPal plus cash, and the footer links /features/passes. Added the live timetable feed students subscribe to, a per-date interest-list FAQ, a refund FAQ and the three appointment payment methods; the title, meta and hero lede lead with "yoga and fitness class schedule", and the .es-flow-scroll week table is untouched. |
| ✅ | ✅ | For Workshop Instructors | `/for-workshop-instructors` | "The Workshop" ground-up rebuild. 2026-09 pass: reviewed and accurate; shared plan band rebuilt. PASS 2: still carried the pre-announcement claim that nothing is mailed automatically, which app:send-event-announcements invalidated; rewritten around the real split (confirmed subscribers get the digest on their own, account followers only ever get a newsletter you write) and pinned by MarketingAudienceClaimTest. ROUND 3: The After class wall still said nobody is told automatically when you add a class (MarketingAudienceClaimTest matches "no" and "nothing", not "nobody"); it now splits the two lists. Six Stripe-only sentences name Stripe and PayPal, and the page gained the live feed, an interest-list bullet and FAQ, a refund FAQ and a /features/passes link; the title and meta lead with workshop registration and class series. |
| ✅ | ✅ | For Visual Artists | `/for-visual-artists` | "The Studio Wall" ground-up rebuild. 2026-09 pass: two places said nothing goes out in your name automatically; new dates now reach confirmed subscribers as a digest that costs nothing from the allowance. PASS 2: FAQ set re-read end to end against the code and found accurate, including the one free appointment type and the 25-photo free cap; no claim needed changing, so the pass went into the shared plan band and the a11y sweep instead. ROUND 3: The Riverside Fair and Fold Projects plates said "No page to link to"; a venue you type in now gets an unclaimed page, shown as a plain logo until claimed. Also corrected: "no seat map anywhere in the product" (seat maps exist on Enterprise venue schedules), event graphics tagged Pro, and three digest sentences now scoped to the dates you add yourself. Added the claim-flow FAQ, Approved Schedules, the live feed, the interest list, PayPal, refunds and the appointment payment methods; the title leads with "artist exhibition calendar". |
| ✅ | ✅ | For Farmers Markets | `/for-farmers-markets` | "The Market" ground-up rebuild. 2026-09 pass: three places said following triggers nothing; new market days now reach the list on their own, batched, with the newsletter still the thing you write. PASS 2: re-read against the code; its selling, submission and digest claims were already exact, so the pass is the a11y sweep and the shared-component upgrades. ROUND 3: Pitch fees read as Stripe-only, and the A-board promised new market days go out on their own, while a recurring season is one event announced once and a trader's accepted submission never reaches the market's digest; both corrected, with PayPal, a payment link and cash named. Added the live feed, refunds (an FAQ plus a pitch-fee card), the interest list on a ticketed harvest supper, and a claimable page for a band named on a market day; title and meta rewritten around a farmers market calendar, vendor applications and pitch fees. |
| ✅ | ✅ | For Hotels and Resorts | `/for-hotels-and-resorts` | "The Concierge" ground-up rebuild. 2026-09 pass: three places said nothing is sent automatically; a guest who left an email now gets next week's additions as one digest. PASS 2: re-read against the code and found accurate throughout, including the promo-code and team-member gates; the pass is the a11y sweep and the shared-component upgrades. ROUND 3: The meta description opened on a hype word ("Elevate") and every paid-experience line read Stripe-only; both fixed, with PayPal, a payment link and cash at the desk named, and FAQ 1 now calls only the sold-out ticket waitlist Pro (the sign-up waitlist is free). Added the live feed, the interest list on both sides of the desk duplex, FAQs on refunding a guest and on hearing when a special dinner goes on sale, viewer door-scanning and free ownership transfer in the team FAQ; the title leads with "Hotel & Resort Guest Activity Calendar". |
| ✅ | ✅ | For Libraries | `/for-libraries` | "The Catalog" ground-up rebuild. 2026-09 pass: two places said nothing is sent automatically when you add a program; new programs now reach the list as a digest, with the newsletter allowance unchanged. PASS 2: re-read against the code; its selling and digest claims were already correct after the Pass 1 sweep. ROUND 3: Paid programs read Stripe-only in three places, the noticeboard-poster bento was badged Pro though graphics are free, and Tracings said an attached author's schedule shows the program at once when a claimed author must accept it first; all corrected. Added the free registration waitlist, the live feed, the interest list for author events (with a plain line that opening free registration sends no on-sale email), refunds, viewer scanning and ownership transfer; the "running total that is Pro" line is untouched, and the title is built around "Library Program Calendar". |
| ✅ | ✅ | For Webinars | `/for-webinars` | "On Air" ground-up rebuild. 2026-09 pass: reviewed and accurate; shared plan band rebuilt. PASS 2: still carried the pre-announcement claim that nothing is mailed automatically, which app:send-event-announcements invalidated; rewritten around the real split (confirmed subscribers get the digest on their own, account followers only ever get a newsletter you write) and pinned by MarketingAudienceClaimTest. ROUND 3: Every Stripe-only sentence now names PayPal, Invoice Ninja, a payment link and cash. Three existing lines were false or inexact: "nothing goes to followers unless you send it" now names the 72-hour digest and says Follow alone signs nobody up; "Cancelling asks the same way" now says cancelling emails as part of cancelling, and registrants get notices only through the schedule's own email settings; the share graphic is Free. Added the interest list (its emails never carry the join link), the live feed (no link in it either) and refunds; the meta description is rewritten around the private join link. Coordinator (hosted email pass): the rack lede says confirmations always go out and only change and cancellation notices need your own SMTP details; the Go on air step and its HowTo twin say the same. |
| ✅ | ✅ | For Live Concerts | `/for-live-concerts` | "Live On Stage" ground-up rebuild. 2026-09 pass: reviewed and accurate; the Twitch and Instagram platform chips keep their real brand colours, which is the documented third-party exemption. PASS 2: re-read; the streaming answer is honest about what the product does not do (no integration, no viewer counts) and the ticketing answers state the ceiling. A11y sweep applied. ROUND 3: The Stripe-only sales copy now names PayPal and the other methods, "25 ticket sales" became "25 paid tickets", the announce graphic is Free, and the line saying buyers are told when a date changes is now exact (a change notice only if sent on saving, a cancellation as part of cancelling, buyers only through the schedule's own email settings). Added the on-sale alert beside the Opens Aug 8 row, the live feed, Google Wallet on the door card, and refunds; the streaming honesty and brand chips are kept; the title is now "Tours & Livestreams". |
| ✅ | ✅ | For Online Classes | `/for-online-classes` | "The Syllabus" ground-up rebuild. 2026-09 pass: reviewed and accurate; shared plan band rebuilt. PASS 2: still carried the pre-announcement claim that nothing is mailed automatically, which app:send-event-announcements invalidated; rewritten around the real split (confirmed subscribers get the digest on their own, account followers only ever get a newsletter you write) and pinned by MarketingAudienceClaimTest. ROUND 3: The FAQ answering "Not automatically" was wrong twice: confirmed sign-ups get the 72-hour digest, and changing a term's time does not prompt a notice (on a recurring event only a link change or cancellation does); rewritten, keeping "account follower" for the Follow-only case. Stripe-only copy names PayPal and the other methods; FAQs added for asking to hear when a course goes on sale and for refunds; students can subscribe to the live feed; the title is now "Terms & Class Cards". |
| ✅ | ✅ | For Virtual Conferences | `/for-virtual-conferences` | "The Agenda" ground-up rebuild. 2026-09 pass: reviewed and accurate; shared plan band rebuilt. PASS 2: still carried the pre-announcement claim that nothing is mailed automatically, which app:send-event-announcements invalidated; rewritten around the real split (confirmed subscribers get the digest on their own, account followers only ever get a newsletter you write) and pinned by MarketingAudienceClaimTest. Two sites, the FAQ and the audience panel. ROUND 3: Four places read as Stripe-only (the takings list, the ticket-types FAQ, the featureList, the close) and now name Stripe or PayPal plus Invoice Ninja, a payment link or cash; the close says 25 paid tickets, and the next-edition card no longer puts share graphics behind Pro (only templates are Pro). Added the interest list as an announce-before-you-sell step, refunds, the live feed, and digest wording scoped to the sessions the organizer posts; new title "Free Virtual Conference Agenda & Ticketing Software". |
| ✅ | ✅ | For Live Q&A Sessions | `/for-live-qa-sessions` | "The Conversation" ground-up rebuild. 2026-09 pass: reviewed and accurate; shared plan band rebuilt. PASS 2: benefits from the shared Pass 2 work - zero WCAG AA contrast failures in both colour modes, no horizontal scroll at 390px, and the "Keep exploring" block now carries the site`s standard lift and brand hairline. Page-specific: the "do my followers get an email" answer was a flat no; corrected to the two-list answer. ROUND 3: Every paid-session line read as Stripe-only (the stat strip, the ticketing row, the charge FAQ, the paid bento, the featureList); they now say Stripe or PayPal, and the paid bento adds refunds. Added the interest list for people not ready to register (a reminder, a cancellation notice, and any change notice the host sends when the link moves) with the live feed, and scoped the digest to sessions the host schedules; the meta and JSON-LD swap "email notifications" for a place limit per date and one join link. |
| ✅ | ✅ | For Watch Parties | `/for-watch-parties` | "The Screening" ground-up rebuild. 2026-09 pass: reviewed and accurate; shared plan band rebuilt. PASS 2: benefits from the shared Pass 2 work - zero WCAG AA contrast failures in both colour modes, no horizontal scroll at 390px, and the "Keep exploring" block now carries the site`s standard lift and brand hairline. Page-specific: the screening-notification answer corrected to the two-list answer. ROUND 3: The take card, the charge FAQ, the R7 lede and fee inset, and the featureList read as Stripe-only; they now say Stripe or PayPal, and R7 adds a refund bullet. The call sheet's automatic mail list now includes the interest-list reminder, the digest answer says confirmed subscribers and screenings you add, and a new FAQ covers reminders without registering plus the live feed; the hero lede opens "A watch party has..." under the poetic H1. Coordinator (hosted email pass): the poll-suggestion email is described as opt-in and dependent on the schedule's own email settings. |
| ✅ | ✅ | For AI Agents | `/for-ai-agents` | "The Console" ground-up rebuild. 2026-09 pass: reconciled the endpoint count with `/open-source` - both are right, and the hero now says which is which (three routes to get a key, 24 behind it); the four machine-readable files re-verified as served. PASS 2: the 27-endpoint ledger is now pinned by MarketingCountedClaimsTest - not just the count, but every printed method and path against routes/api.php, because the reader of this page is an agent about to call them. ROUND 3: PUT /api/sales/{id} now refunds, so the ledger row, the settle-a-sale caption, the Money card, the webhook list (sale.refunded is a full refund, a partial fires nothing, sale.paid names Payfast), the 409 status, the featureList and a new refund FAQ (amount, idempotency_key, 409, full-only installments) say what it does, and the payment_method chips match the API reference. Added a Pro-gate FAQ checked against every controller and a Pro note on step 03, because a new hosted schedule gets a 403 there. llms-full.txt recounted at 2,077 lines (was 1,906); llms.txt stays at 39. |

## Comparison / "Alternative" pages (18)

| Reviewed | Updated | Page | URL | Notes |
|:--------:|:-------:|------|-----|-------|
| ✅ | ✅ | Compare (hub) | `/compare` | "Head to Head" ground-up rebuild. 2026-09 pass: the 42-row master grid billed Ticketing and QR check-ins as Pro; both are Free, an "Unlimited ticket sales" row now carries the Pro half (43 rows), and the calendar-sync row names all three carriers. PASS 2: contrast clean; the comparison table wrapper is now a containing block so its sr-only cell labels stop widening the page on a phone. ROUND 3: The grid billed event graphics as Pro (GraphicController checks membership only) and AI parsing as Enterprise (parsing runs on every plan); our side of both is corrected in getHubComparisonData(). Two rows were added with every competitor cell checked against that competitor's own help pages, "Refunds, full or partial" and "Reserved seating" (45 rows). Competitor cells re-verified on 2026-09-10 and corrected: Eventbrite and Luma (Plus) have webhooks, Luma has coupons, embeds a whole calendar and offers unlisted (not password-protected) events, and Luma's recurring events are clone-only. Eventbrite's pricing page adds a 2.9% payment processing fee per order on top of the 3.7% + $1.79 service fee, so the processing cell (was "included above"), getHubFeeRates() and the calculator now include it (100 tickets at $10: $245, was $216) and the footnote says so. Three FAQs answer PayPal and refunds, reserved seating, and the multi-event cart and installments; the switching band states the Pro gates and links the Eventbrite move page; title "Compare Event Schedule vs Eventbrite, Luma & 14 More" (52), description 154. |
| ✅ | ✅ | Eventbrite Alternative | `/eventbrite-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes (all 16 pages): our payment cell names Stripe, PayPal and Payfast (12 pages said Stripe only); event graphics are Free; the AI row reads "Parsing on Free, flyer and style on Enterprise" instead of billing parsing as Enterprise; multipliers computed on our own price ("12x", "10x", "7x", "125x", "save $145") became plan_price() pairs or competitor-only figures; sync cards name Outlook; every page has its own meta_title (56 to 63) and a description of 131 to 155; migration step 2 gives the real AI parse limits (50 a day on Free and Pro, 10 during a trial, 100 on Enterprise) and step 4 is "Connect Stripe or PayPal and sell"; the default why-choose list adds refunds, the cart, the interest list, passes, gift cards and installments (Pro) and seating (Enterprise); the JSON-LD offers moved graphics from Pro to Free. This page: Eventbrite's US pricing page charges a 2.9% payment processing fee per order on top of the 3.7% + $1.79 service fee, so the processing cell (was "included above"), fee card and pricing FAQ now say so. Why-choose gained PayPal, the cart, refunds, seating and the interest list with their plans, the importer is labelled Pro, "Unique to Event Schedule" came off the AI card, and two FAQs were added (refunds from the Sales page, reserved seating on Enterprise). Coordinator: the Webhooks cell said Eventbrite has none; Eventbrite documents webhooks on its developer platform, so the cell is now Yes and no longer counted as an edge. |
| ✅ | ✅ | Move from Eventbrite | `/switch-from-eventbrite` | "The Move" ground-up rebuild, 2026-09-09. A MIGRATION page, not a comparison, and filed here because it is the sibling of `/eventbrite-alternative` and the two now link to each other (the alternative page's related strip swapped Luma for this rather than growing to a ragged five). Comparison pages are read while deciding; this is read while doing, so the fee arithmetic deliberately stays on `/compare`, which derives every rate from one array. The packing list states exactly what `EventbriteController` carries across and what it does not - past orders and attendee history do not travel, and the honest answer is the bulk attendee import. The Eventbrite link-back is set for online events only, and the page says so. ROUND 3: The packing list and FAQ left out two things EventbriteController does carry, the event description and ticket quantities; both are in now. A third gaps card names what stays behind: discount codes, checkout questions and seat maps, and every event arriving as a one-time date. The bulk attendee import is marked Pro, and the page says orders taken on Eventbrite are refunded on Eventbrite. New cards and three FAQs cover refunds from the Sales page (Stripe and PayPal move the money; the buyer is not emailed), PayPal on every plan and the interest list, linking /paypal, /features/ticketing and the /docs/tickets anchors. Title "Switch from Eventbrite to Event Schedule: Migration Guide" (57), description 149, FAQPage 10/10 visible. |
| ✅ | ✅ | Luma Alternative | `/luma-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. The meta description sold custom domains at the Pro price (they are Enterprise) and called the product "powerful"; both rewritten. "12x More Affordable" became a price pair, Luma Plus reads "$59/mo, billed annually" as Luma's pricing page states, FAQ 1 points the audience to the email digest and the live feed instead of "calendar sync", and a new FAQ covers the interest list. Coordinator: four Luma cells re-checked against help.luma.com on 2026-09-10 and corrected: coupons (Promo codes "Yes (coupons)"), webhooks on Luma Plus, events can be unlisted but not password-protected, and recurring events are clone-only (no repeating series). |
| ✅ | ✅ | Ticket Tailor Alternative | `/ticket-tailor-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. The meta description promised "AI features for a flat $5/mo" (flyer generation is Enterprise); it now carries the selling facts: no per-ticket fee, Stripe or PayPal, refunds, the cart and 25 paid tickets a month free. Why-choose marks AI flyer generation Enterprise and adds checkout, refunds, gift cards, passes, installments and seating, "robust" came out of the strengths, and a new FAQ answers gift cards, passes, payment plans and seating with each plan named. |
| ✅ | ✅ | Google Calendar Alternative | `/google-calendar-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. Its comparison table also carried its own older vocabulary - Ticket sales, QR code check-in, Sub-schedules/categories - and with it two tiers that stopped being true when the free plan started selling; realigned on the labels its fifteen siblings use, which also gives the /compare hub a real Ticketing row for it. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. FAQ 2 listed team collaboration on the free plan (one member only) and FAQ 3 said "Event Schedule Pro includes full ticketing"; both now say selling starts free through Stripe or PayPal and Pro lifts the 25-a-month cap. Added the live calendar feed (why-choose, plus a new FAQ on subscribing from your own calendar), refunds on the ticketing card, and a new description. |
| ✅ | ✅ | Meetup Alternative | `/meetup-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. The meta description offered custom domains (Enterprise) as a way to avoid subscription costs; it now leads with free community pages, email sign-ups, the live feed and ticketing, and the community card names Enterprise for your own domain. A new FAQ explains how members hear about events: the digest (at most one every 72 hours), the live feed, the interest list, and newsletters within the recipient allowance. |
| ✅ | ✅ | DICE Alternative | `/dice-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. The payouts card and FAQ 2 name Stripe or PayPal, with refunds going back the same way. A new FAQ says tickets open in any browser from the email or ticket page with their QR code, plus Add to Google Wallet where the install has Wallet switched on, and the description was rewritten around that. |
| ✅ | ✅ | Brown Paper Tickets Alternative | `/brown-paper-tickets-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. The description was 109 characters; it now leads with BPT's own retirement notice ("The Brown Paper Tickets website is being retired") and the free plan's 25 paid tickets a month. "Fast Stripe Payouts" became "Direct Payouts" through Stripe or PayPal, FAQ 2 states the free allowance, and a new FAQ covers several shows in one order. Coordinator: the about text's "no new events for 2026 onward" is not on BPT's site; it now says what the site says, that it is being retired and sends organizers to Events.com for their next event. |
| ✅ | ✅ | Splash Alternative | `/splash-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. FAQ 3 put custom CSS and API access on Enterprise; both are Pro, and it now says custom domains and team members are Enterprise. "Powerful" came out of the description, and a new FAQ covers teamwork (Enterprise, up to five members on hosted; admins see sales, viewers scan tickets). |
| ✅ | ✅ | Sched Alternative | `/sched-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. "10x More Affordable" became a plain Pro price per month and the sync card names Outlook. A new FAQ covers one pass for several sessions (Pro: festival, visit, membership and season passes) and the multi-event cart on every plan. Open: Sched's "$50/mo" could not be re-verified (its pricing page renders a placeholder). |
| ✅ | ✅ | Whova Alternative | `/whova-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. FAQ 3 now marks polls and post-event feedback as Pro, and a new FAQ covers refunding an attendee from the Sales page, in full or in part, through Stripe or PayPal. |
| ✅ | ✅ | Accelevents Alternative | `/accelevents-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. The card quoted three prices that disagreed ($7,000/year, $7,500+/year and "125x") and a $1 + 1% ticket fee. Accelevents' pricing page lists plans from $7,500 (Professional) and $13,500 (Business), no free plan, "No transaction fees" and processing through Stripe, Square and Payflow, so the free plan, price, platform fee and processor cells, the price card, description and FAQ were corrected; the fee line is now a match, not an edge. A new FAQ covers seating (Enterprise) and installments (Pro, Stripe). |
| ✅ | ✅ | Tito Alternative | `/tito-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. The "$145/mo" saving was arithmetic on our own price; the card now shows only Tito's verified 3% per ticket (200 tickets at $30 is $180). The custom-domain card names Enterprise, and a new FAQ covers taking PayPal next to Stripe, with refunds through either. |
| ✅ | ✅ | AddEvent Alternative | `/addevent-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. The card said AddEvent had "Unlimited ticket sales: Yes" while its Ticketing row said No; AddEvent's pricing lists no ticketing, so that cell is N/A. Our "Add to calendar buttons" and "Subscription calendars" cells read "Via embeds", but every event page has Add to Calendar and every schedule has a live feed, so both are Free. The price is AddEvent's verified $36/mo on monthly billing, and a new FAQ covers subscription calendars. |
| ✅ | ✅ | Pretix Alternative | `/pretix-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. A new FAQ says seating plans are on Enterprise and that a selfhosted install resolves to Enterprise; pretix's seating strength is kept as written. |
| ✅ | ✅ | Humanitix Alternative | `/humanitix-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. FAQ 3 names Stripe or PayPal processing, and a new FAQ covers installments: Pro, Stripe only, the first payment at checkout, an arrears hold at the door, and refunds in full only. |
| ✅ | ✅ | Eventzilla Alternative | `/eventzilla-alternative` | Renders the rebuilt `compare-single` "The Scorecard" template. 2026-09 pass via the shared template and `getComparisonData()`: the grid billed Ticketing and QR check-ins as Pro on every one of the sixteen cards - selling is free to 25 paid tickets a month and scanning has no plan check - so those rows are now Free, a new "Unlimited ticket sales" row carries the Pro half, and the Google-only sync row was renamed to name Outlook and CalDAV. PASS 2: the switch section, which is the one section a reader on an alternative page has actually come for, said three generic lines and now names four real routes out of the tool being left - EventController::parse for the events with its per-tier daily limit, NewsletterController::importStore for the list at ten thousand rows and no plan gate, and Role::ticketSaleLimit() for what selling costs on day one. The why-choose default was rewritten as facts with their tiers attached (it ran Enterprise flyer generation and Pro event graphics together into one free-sounding line), and its four-card grid now completes instead of leaving a lone card on a second row. ROUND 3: Shared compare-single changes as on /eventbrite-alternative. FAQ 3 listed custom fields, the check-in dashboard, the waitlist and export with no plan; it now says ticketing and QR check-in are on every plan and the rest are Pro. A new FAQ covers several events in one order. |

## "Replace" pages (13)

| Reviewed | Updated | Page | URL | Notes |
|:--------:|:-------:|------|-----|-------|
| ✅ | ✅ | Replace (hub) | `/replace` | "The Toolbelt" ground-up rebuild; hub for the twelve replacement singles. 2026-09 pass: reviewed, and the tier claims behind the singles were corrected at source in `getReplacementData()`. PASS 2: still carried the pre-announcement claim that nothing is mailed automatically, which app:send-event-announcements invalidated; rewritten around the real split (confirmed subscribers get the digest on their own, account followers only ever get a newsletter you write) and pinned by MarketingAudienceClaimTest. ROUND 3: The plan cards and the pricing FAQ still put QR check-in and event graphics on Pro (both are free; only the live check-in dashboard is Pro), the bench card called the digest the one thing that sends itself, and the money copy was Stripe-only. Added the new jobs where a tool-replacer looks: the interest list, the sign-up digest, short links with counted clicks, refunds and appointment payment methods in the Forms, Mailchimp, Linktree, Sheets and Calendly rows, PayPal and refunds on the zero-fee card, a live calendar feed card, and FAQs on replacing a notify-me form and a mailing-list sign-up; title "Replace Google Forms, Canva & 10 More Tools". |
| ✅ | ✅ | Google Forms Replacement | `/google-forms-replacement` | Renders the rebuilt `replace-single` "The Swap" template. 2026-09 pass via the shared template: the swap list said tickets with QR check-in are Pro, now split into the free 25-a-month allowance with door scanning and the Pro ceiling plus live dashboard; the "Pro adds ticketing" FAQ answers behind these pages were corrected at source. PASS 2: FAQ set, comparison rows and switch steps re-read against the code; the tier claims here were already exact after the Pass 1 sweep, and the pricing note was checked against Role::ticketSaleLimit() for the price-first shape ("$5/month for ticketing") that a plan-name grep cannot see. ROUND 3: Shared replace-single changes (all 12 pages): each page has its own meta_title (50 to 60; they were all "Replace X for Events" under 50); the plan cards and JSON-LD offers stopped listing event graphics, "ticketing with QR check-in" and "Stripe payments" as Pro and now show 25 paid tickets a month scanned free at the door, every payment method and refunds on Free, and newsletter allowances in recipients; the footnote lists all six payment methods, Stripe or PayPal refunds and the digest outside the newsletter allowance; "Nothing to migrate" became "No migration project". This page: the pricing note's "registration, payments, emails and check-in from $X/month" shape is gone; Google Forms' own help shows it can email respondents a copy of their answers, so the confirmation row and pain point were corrected; the duplicate RSVP FAQ became a "notify me" FAQ for the free interest list with a matching card; payment methods, free door scanning and Pro custom fields are stated; title "Replace Google Forms for Event Registration"; related link AI Import became Check-in. |
| ✅ | ✅ | Mailchimp Replacement | `/mailchimp-replacement` | Renders the rebuilt `replace-single` "The Swap" template. 2026-09 pass via the shared template: the swap list said tickets with QR check-in are Pro, now split into the free 25-a-month allowance with door scanning and the Pro ceiling plus live dashboard; the "Pro adds ticketing" FAQ answers behind these pages were corrected at source. PASS 2: the migration step for the one thing a Mailchimp reader is actually moving - the list - said their attendees would subscribe again through the schedule page. NewsletterController::importStore takes ten thousand rows from a paste or a CSV with no plan gate, so the step now says so. ROUND 3: Shared replace-single changes as on /google-forms-replacement. The FAQ still said "you do not need to import a CSV", contradicting the import step; it now describes the 10,000-row paste or CSV import. "Ticket buyers become subscribers automatically" was false (checkout sign-up is a tick box), so that card became the automatic new-event digest. Pricing and automation answers state the allowance in recipients with the digest outside it; added the Ticket buyers segment, A/B tests on subject or content and a double opt-in FAQ. Title "Replace Mailchimp for Event Newsletters". |
| ✅ | ✅ | Canva Replacement | `/canva-replacement` | Renders the rebuilt `replace-single` "The Swap" template. 2026-09 pass via the shared template: the swap list said tickets with QR check-in are Pro, now split into the free 25-a-month allowance with door scanning and the Pro ceiling plus live dashboard; the "Pro adds ticketing" FAQ answers behind these pages were corrected at source. PASS 2: FAQ set, comparison rows and switch steps re-read against the code; the tier claims here were already exact after the Pass 1 sweep, and the pricing note was checked against Role::ticketSaleLimit() for the price-first shape ("$5/month for ticketing") that a plan-name grep cannot see. ROUND 3: Shared replace-single changes as on /google-forms-replacement. The pricing note and the "Is Event Schedule free for event graphics?" FAQ said graphics are Pro; they are free on every plan, so both were corrected, and AI flyers, AI style and AI-written graphic text are marked Enterprise wherever they appear. The description stopped calling the free graphics "AI-generated". Title "Replace Canva for Event Flyers and Graphics". |
| ✅ | ✅ | Linktree Replacement | `/linktree-replacement` | Renders the rebuilt `replace-single` "The Swap" template. 2026-09 pass via the shared template: the swap list said tickets with QR check-in are Pro, now split into the free 25-a-month allowance with door scanning and the Pro ceiling plus live dashboard; the "Pro adds ticketing" FAQ answers behind these pages were corrected at source. PASS 2: FAQ set, comparison rows and switch steps re-read against the code; the tier claims here were already exact after the Pass 1 sweep, and the pricing note was checked against Role::ticketSaleLimit() for the price-first shape ("$5/month for ticketing") that a plan-name grep cannot see. ROUND 3: Shared replace-single changes as on /google-forms-replacement. The page never said what happens to a creator's other links: it now covers the short address each link gets on the schedule's own URL, with clicks counted in analytics (new card, comparison row, step, why-switch point and FAQ). The pricing note said Pro "buys" the page, ticketing, newsletters and sign-ups, all of which are free; the sign-up answer describes the real name-and-email flow, digest, account and calendar feed; branding is tiered correctly. Title "Replace Linktree for Your Event Link in Bio"; related link Embed Calendar became Analytics. |
| ✅ | ✅ | Google Sheets Replacement | `/google-sheets-replacement` | Renders the rebuilt `replace-single` "The Swap" template. 2026-09 pass via the shared template: the swap list said tickets with QR check-in are Pro, now split into the free 25-a-month allowance with door scanning and the Pro ceiling plus live dashboard; the "Pro adds ticketing" FAQ answers behind these pages were corrected at source. PASS 2: its team-collaboration answer and card promised role-based access with no tier attached; both now name the Enterprise plan and the five-member cap. ROUND 3: Shared replace-single changes as on /google-forms-replacement. The live dashboard card claimed revenue, but CheckInController::stats returns sold, checked-in and recent arrivals only; fixed, with the dashboard, CSV export and REST API marked Pro. The duplicate REST API card became a free "Refunds, Recorded" card; added a Refunds row, a refunds FAQ, a Pro guest-list import FAQ (5,000 rows), and PayPal and the multi-event cart in the steps; the Enterprise five-member team note gained admin and viewer roles. Title "Replace Google Sheets for Event Tracking". |
| ✅ | ✅ | Calendly Replacement | `/calendly-replacement` | Renders the rebuilt `replace-single` "The Swap" template. 2026-09 pass via the shared template: the swap list said tickets with QR check-in are Pro, now split into the free 25-a-month allowance with door scanning and the Pro ceiling plus live dashboard; the "Pro adds ticketing" FAQ answers behind these pages were corrected at source. This page also gained an Appointment Booking card, since booking is exactly what a Calendly user is replacing and we ship it free with one type. PASS 2: the page argued about public event pages and never answered the question a Calendly reader arrives with. Two FAQs added from the appointment code: booking is free with one type (hours, interval, buffers, overrides, optional approval, Stripe or link or cash, and nothing bookable over something already on the schedule), and a reschedule MOVES the booking so the payment, private link and calendar entry carry over. ROUND 3: Shared replace-single changes as on /google-forms-replacement. Calendly's own help and pricing pages show it takes Stripe or PayPal payments at booking, so "Calendly cannot process event payments" and the payments pain point were corrected (cell: "Booking payments only"). The price-first "ticketing at $X/month" note was fixed, step two became "Set your bookable hours", and a Sales-page refund FAQ, the three booking payment rails ("PayPal and Payfast are for tickets, not bookings") and the live calendar feed were added; promo codes dropped from the free selling line. Title "Replace Calendly for Bookings and Events"; related link Recurring Events became Appointments. |
| ✅ | ✅ | SurveyMonkey Replacement | `/surveymonkey-replacement` | Renders the rebuilt `replace-single` "The Swap" template. 2026-09 pass via the shared template: the swap list said tickets with QR check-in are Pro, now split into the free 25-a-month allowance with door scanning and the Pro ceiling plus live dashboard; the "Pro adds ticketing" FAQ answers behind these pages were corrected at source. PASS 2: FAQ set, comparison rows and switch steps re-read against the code; the tier claims here were already exact after the Pass 1 sweep, and the pricing note was checked against Role::ticketSaleLimit() for the price-first shape ("$5/month for ticketing") that a plan-name grep cannot see. ROUND 3: Shared replace-single changes as on /google-forms-replacement. The ticket waitlist and promo codes had no tier; they are marked Pro, with the RSVP waitlist free. SurveyMonkey's pricing lists payment collection as a plan feature, so the "Paid add-on" cell and pain point were corrected. The promo card became a post-event feedback card and the survey FAQ explains how feedback is collected (Pro; on eventschedule.com only schedules with their own email settings send the requests) plus Pro polls; added a Post-event feedback row and an interest-list FAQ. Title "Replace SurveyMonkey for Event Registration"; related link Polls became Feedback. |
| ✅ | ✅ | Doodle Replacement | `/doodle-replacement` | Renders the rebuilt `replace-single` "The Swap" template. 2026-09 pass via the shared template: the swap list said tickets with QR check-in are Pro, now split into the free 25-a-month allowance with door scanning and the Pro ceiling plus live dashboard; the "Pro adds ticketing" FAQ answers behind these pages were corrected at source. PASS 2: FAQ set, comparison rows and switch steps re-read against the code; the tier claims here were already exact after the Pass 1 sweep, and the pricing note was checked against Role::ticketSaleLimit() for the price-first shape ("$5/month for ticketing") that a plan-name grep cannot see. PASS 2: its pricing note said Event Schedule is free for events and $5/month for ticketing, which is the same wrong claim as "ticketing is Pro" with the plan name taken out. Corrected against Role::ticketSaleLimit() and pinned: MarketingTicketingTierTest now reads the RENDERED fixtures as well as the source, because the controller concatenates plan_price() into the middle of these sentences and a source scan cannot see them. ROUND 3: Shared replace-single changes as on /google-forms-replacement. Accurate, so small edits: the AI-import card became "A Calendar Feed for Your Group", the sign-up card states the name-and-email digest, a new FAQ covers getting dates into the group's own calendars, and the RSVP answer gained the free RSVP waitlist. Description trimmed to 152; title "Replace Doodle for Event Scheduling"; related link AI Import became Recurring Events. |
| ✅ | ✅ | QR Code Generator Replacement | `/qr-code-generator-replacement` | Renders the rebuilt `replace-single` "The Swap" template. 2026-09 pass via the shared template: the swap list said tickets with QR check-in are Pro, now split into the free 25-a-month allowance with door scanning and the Pro ceiling plus live dashboard; the "Pro adds ticketing" FAQ answers behind these pages were corrected at source. PASS 2: the pricing answer said QR tickets are included in Pro and that the free plan is events without ticketing - both wrong since the free tier started selling. Rewritten around the real split: 25 paid tickets a month free with scanning ungated, Pro for the ceiling and the live dashboard. ROUND 3: Shared replace-single changes as on /google-forms-replacement. The pricing note still said QR codes come "with the Pro plan", which also "covers ticketing, check-in"; it now states free scanning, the 25-a-month allowance and the Pro dashboard. The dashboard, sale-notification and platform-fee cards are tiered correctly with all payment methods (the fee card had claimed "all ticketing features" included and "only Stripe" processing). Scanning is described accurately (browser-based, admits once, re-scan warns, refunded or cancelled refused), with a refused-tickets row and a Google Wallet FAQ as a buyer convenience. Title "Replace QR Code Generators for Check-in"; related link Analytics became Check-in. Coordinator (hosted email pass): the Sale Notifications card says the first-sale email needs the schedule's own email settings on eventschedule.com. |
| ✅ | ✅ | Squarespace Replacement | `/squarespace-replacement` | Renders the rebuilt `replace-single` "The Swap" template. 2026-09 pass via the shared template: the swap list said tickets with QR check-in are Pro, now split into the free 25-a-month allowance with door scanning and the Pro ceiling plus live dashboard; the "Pro adds ticketing" FAQ answers behind these pages were corrected at source. PASS 2: FAQ set, comparison rows and switch steps re-read against the code; the tier claims here were already exact after the Pass 1 sweep, and the pricing note was checked against Role::ticketSaleLimit() for the price-first shape ("$5/month for ticketing") that a plan-name grep cannot see. PASS 2: same price-first ticketing claim as /doodle-replacement, corrected the same way. Its statement about Squarespace's own pricing is untouched, and the test that catches this is anchored on our own name so a competitor fact never trips it. ROUND 3: Shared replace-single changes as on /google-forms-replacement. Squarespace's pricing lists its own Email Campaigns and a 2% transaction fee on the Personal plan only, so the "Newsletters: third-party plugins" and "Transaction fees" cells and the pain point were corrected. "Squarespace owns your site if you cancel" could not be verified and was replaced with what Squarespace's export help states (calendar and store pages do not export), beside our free backup export. Added PayPal, the multi-event cart, a payments and refunds FAQ (with the Pro sales export) and the embed and white-label tiers; "seamlessly" is gone. Title "Replace Squarespace for Event Pages"; related link White Label became PayPal. |
| ✅ | ✅ | Notion Replacement | `/notion-replacement` | Renders the rebuilt `replace-single` "The Swap" template. 2026-09 pass via the shared template: the swap list said tickets with QR check-in are Pro, now split into the free 25-a-month allowance with door scanning and the Pro ceiling plus live dashboard; the "Pro adds ticketing" FAQ answers behind these pages were corrected at source. PASS 2: said the free plan includes team features and team collaboration, in the pricing note and in an FAQ answer; a free schedule is a single account and five members is Enterprise. Corrected in all three places, and the comparison row now carries the tier the way the /compare tables do. ROUND 3: Shared replace-single changes as on /google-forms-replacement. The free-plan FAQ said Pro "adds event graphics", but graphics are free. Notion's pricing shows it publishes pages to the web and has Notion Calendar, so the public-pages and calendar-sync cells, the about text and the pain points now draw the true line (a published page is a document, not an event page with tickets). The Enterprise five-member team note gained admin and viewer roles; added a Pro event-templates FAQ, PayPal and the REST API marked Pro. Title "Replace Notion for Event Planning". |
| ✅ | ✅ | Trello Replacement | `/trello-replacement` | Renders the rebuilt `replace-single` "The Swap" template. 2026-09 pass via the shared template: the swap list said tickets with QR check-in are Pro, now split into the free 25-a-month allowance with door scanning and the Pro ceiling plus live dashboard; the "Pro adds ticketing" FAQ answers behind these pages were corrected at source. PASS 2: two errors in one sentence - the free plan does not include team collaboration and ticketing is not what $5 buys. Both corrected; the switch step that said "invite your team" now names the Enterprise plan and the five-member cap, which is the one place a board of collaborators does not map straight across. ROUND 3: Shared replace-single changes as on /google-forms-replacement. The free-plan FAQ said Pro "adds event graphics"; graphics are free, so it was corrected. The submissions card and a new FAQ say submitting is free on every plan, submitters are told when declined, and custom questions on the form are Pro; the team step gained admin and viewer roles and PayPal. Title "Replace Trello for Event Management". |

## Legal / Policy (4)

| Reviewed | Updated | Page | URL | Notes |
|:--------:|:-------:|------|-----|-------|
| ✅ | ✅ | Privacy Policy | `/privacy` | "The Fine Print" family treatment (shared restrained legal set); legal text verbatim. 2026-09 pass: reviewed, clause numbering and anchors intact, and the page passes the structural sweep clean. PASS 2: es_attribution was undisclosed. It is written on every marketing page for a signed-out visitor and is deliberately NOT gated on consent, because it stands in for what the session cookie held before anonymous marketing HTML became edge-cacheable - which is exactly why a policy has to name it. Disclosed in the clause and added to the identifier list. ROUND 3: Clause 06 now lists the two new ways details arrive without an account: the interest list (an email plus the IP address and page language the form records, deleted on unsubscribe or with the event) and pages an organizer creates for an act or venue (contact details never shown in full). Clause 08's erasure step points at Settings rather than a Profile menu that no longer exists and names the exits for data held without an account, clause 12 says a confirmed email sign-up now creates an account that follows the schedule, and the Google Wallet processor row lists every field the pass carries. Legal wording and clause numbers untouched; processor-register gaps that predate this round are listed for the owner in the round summary. |
| ✅ | ✅ | Terms of Service | `/terms-of-service` | "The Fine Print" family treatment; legal text verbatim. 2026-09 pass: reviewed, sixteen clauses intact and citable, structural sweep clean. PASS 2: the clause index every reader taps to navigate measured exactly 23px, one pixel under the WCAG 2.2 target minimum, because 0.72rem x line-height 2 lands there; padding-block fixes it without changing the rhythm of the column. ROUND 3: Checked for any description of ticketing, refunds, payouts or fees; the instrument contains none, so nothing changed. Three legal points raised by new features (the Hosts and Influences account types in 01, the no-access sentence in 07 beside the admin schedule tools, and acceptance now that a confirmed sign-up creates an account) are reported to the owner in the round summary rather than edited. |
| ✅ | ✅ | Accessibility | `/accessibility` | "The Fine Print" family treatment; legal text verbatim. 2026-09 pass: reviewed. The statement invites exactly the check this campaign ran, so the site-wide structural sweep behind it is recorded here: 37 heading-level skips fixed across the docs and one on /for-curators, and every page now has one h1, a clean outline, no unlabelled control and valid JSON-LD. The Last reviewed date is the operator`s to move, so it was left alone. PASS 2: the declaration now publishes a measured record instead of only a good-faith undertaking - 157 public pages, both themes, on a stated date: no contrast below WCAG 2.1 AA, every control with an accessible name, no target under the WCAG 2.2 24px minimum, no horizontal scroll at 390px, reduced-motion honoured everywhere. Scoped to the public site, with the admin and guest portals explicitly excluded and clause 06 untouched. Numbers live in config/accessibility.php; the sentence is translated into all twelve languages. ROUND 3: Every product description re-checked against the code (the display panel's four controls, the Advanced setting that turns it on, where it renders, per-device storage and the signed-in hide) and all still hold; the measured record and its twelve translations untouched, so no change. |
| ✅ | ✅ | Self-Hosting Terms of Service | `/self-hosting-terms-of-service` | "The Fine Print" family treatment; legal text verbatim. 2026-09 pass: reviewed; the hosted-terms footnote linked `/terms`, which 302s, and now points straight at `/terms-of-service`. PASS 2: the at-a-glance panel said every Pro and Enterprise feature is included, which is true and incomplete - it now names the two things that exist ONLY on a selfhosted install, one-click updates and importing events from a URL or by city. ROUND 3: The at-a-glance panel called the selfhost-only importer "importing events from a URL or by city"; it now describes Auto Import as a daily read of pages you choose with an optional city filter. PayPal joined Stripe in the Payments row, the Anything you connect card and the cost line, the same card lists Google Wallet passes, and the boundary footnote no longer claims only three rows ever leave the server. |

## Docs - User Guide (20)

> The docs deliberately share one restrained shell (`config/docs.php` + `<x-docs-page>`, rebuilt July 2026) rather than per-page designs.

| Reviewed | Updated | Page | URL | Notes |
|:--------:|:-------:|------|-----|-------|
| ✅ | ✅ | Docs Home | `/docs` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. The glossary now separates account followers from email subscribers, because only one of the two lists gets the automatic digest. PASS 2: the three-step "start here" now says what collecting an email address actually earns the reader - subscribers who hear about new events on their own - rather than "followers you can email later"; the glossary already gained the follower/subscriber split in pass 1. ROUND 3: The glossary now has fifteen terms, so the 3-up grid closes again: it gains "Interest list" (per event, email only, not a subscription) and "Unclaimed page" (created when you name an act or venue, kept out of search, claimable). "Ticket" names every payment method instead of Stripe only, "Subscriber" says the sign-up panel takes a name and email and confirming creates an account, "Request" covers events another schedule adds you to, and "Event" lists every repeat pattern. The start-here step says the free plan sells 25 paid tickets a month through Stripe, PayPal or another method; title 55, description 155. |
| ✅ | ✅ | Getting Started | `/docs/getting-started` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Two FAQ entries added and mirrored in the FAQPage JSON-LD (9/9): how you get paid (every payment method, refunds from the Sales page) and how people hear about new events (subscriber digest, calendar feed, interest list). The claim entry now follows the real flow: signed out goes to sign-up, a different account sees the contact masked, and the schedules that already listed you keep listing you. Removed the stale "event graphics are Pro" and the "city search" wording for auto import, added Event Sources to the Customize table and the Curator bullet, and noted that a confirmed email subscriber already has a passwordless account the sign-up form completes. Title 60, description 152; the FAQ search row updated. |
| ✅ | ✅ | Creating Schedules | `/docs/creating-schedules` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: one hand-written <pre> missed the scroll wrapper every other docs code block has, and pushed the page 28px wide on a phone. ROUND 3: Notifications rewritten against the code: the toggles carry the app's own labels; each owner and admin sets their own and the emails go to that person's account address (not the contact email, which the Contact Info field no longer claims); the sixth toggle, "Installment payments", is documented (it and "New event requests" start on); sale emails past an event's first paid sale are Pro; the subscriber digest covers only events the schedule created. Short links: a brand name is used only while free, a custom address replaces it for other sites while platform addresses keep working, and clicks show per link without your team's. CalDAV has no delete-sync setting and the sale-or-boost guard is stated, feeds point to the guest "Subscribe to all events from" link, the Talent Requests tab lists its three settings, and Event Sources joins the section table. Template variables still match parseTemplate() and the /docs/event-graphics twin. Title 58; the Notifications and Auto Import search rows fixed (Auto Import was described as an iCal feed). |
| ✅ | ✅ | Schedule Styling | `/docs/schedule-styling` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Custom CSS now names its real scope (pages drawn in the schedule's own style, including embeds, booking, requests and gift cards, but not the ticket, order or installment pages) and says saved CSS stops being applied below Pro. The social-preview line no longer claims a generic Event Schedule fallback image, the white-label paragraph notes that hosted event graphics carry an eventschedule.com credit on every plan, and "all changes preview in real time" became "most" (custom CSS does not preview). Title 60; description and lede rewritten. |
| ✅ | ✅ | Managing Schedules | `/docs/managing-schedules` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Requests no longer says a declined paid booking is "reminded to refund it yourself": declining cancels the sale without returning the money, the cancellation email gives the amount and reference, and Refund Ticket on the Sales page is offered only before you decline. The Team table now gives admins sales, the waitlist, the check-in dashboard and refunds, and viewers door scanning only (with the Curator exception). Requests explains that a Talent schedule reviews every date another schedule adds it to, and Followers names the three panels, the signed-out Follow dialog, the digest's scope (only events the schedule created), the QR code opening the sign-up form, and selfhost accounts. Meta description and the Team search row rewritten. Coordinator: docs title override added so the tab and search result say what the page covers: "Managing Schedules: Team, Requests, Followers - Event Schedule" (62). Coordinator (hosted email pass): accepting or declining a booking says every email about it, to the guest or to you, needs the schedule's own email settings on eventschedule.com. |
| ✅ | ✅ | Creating Events | `/docs/creating-events` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: The "Notifying attendees" callout became a Notifying Attendees of Changes subsection (#notify-attendees, with a search row) quoting both dialogs: buyers are emailed only when the schedule has its own email settings, the interest list either way; Cancel and notify always sends; cancelling refunds nobody. Claim and Participants cover the whole lineup and the rule that a claimed Talent schedule always reviews new dates (the "turn Require approval off" line is gone). Tickets lists every payment method, the interest count and the free door scanner; Internal & Unlisted adds the digest, feed and interest sign-up per state plus the send-time re-check; Polls says only members vote on hidden events. Six search rows fixed (the WhatsApp and AI details generator rows were wrong). Coordinator: docs title override added so the tab and search result say what the page covers: "Creating Events: Tickets, Recurring, Privacy - Event Schedule" (61). |
| ✅ | ✅ | Sharing | `/docs/sharing` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. The "Following Is Not a Notification Subscription" callout became "Following and Subscribing Are Different Lists", because subscribers now do get an automatic digest. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Calendar Subscriptions no longer says Add to Calendar "is not a subscription". Finding Your Feed URLs covers both guest routes (the "Subscribe to all events from" row and the Stay up to date panel), the Following page and Integrations > Advanced, and the feed list adds internal events, accepted-only and cancelled events dropping out. Building Followers was rebuilt around the signed-out Follow dialog with a new Email Sign-Ups subsection, the three panels and a privacy note on what subscribers and the team see. The QR code opens the sign-up form, "graphics are Pro" became free (AI text and scheduled emails Enterprise), and the meta, lede, HowTo JSON-LD (new feed step) and three search rows were updated. Coordinator: docs title override added so the tab and search result say what the page covers: "Sharing Your Schedule: Links, Embeds, Feeds - Event Schedule" (60). |
| ✅ | ✅ | Tickets | `/docs/tickets` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. Reviewed; the 5,000-row import cap and the "scanning is free, the dashboard is Pro" callout both verified in code. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: The interest list quotes its UI labels and says exactly what is automatic: the on-sale email, the 48-hour reminder and the cancellation notice from Cancel event; a change notice goes only if the organizer chooses Notify attendees, and above 50 waiting on hosted nothing sends without the schedule's own email settings or a verified owner phone. New #refunds section (nav link and search row): Refund Ticket or Mark as Refunded, partial refunds (the sale stays paid), installment plans refunded in full only, refunds made in the Stripe or PayPal dashboard never reported back, no email to the buyer, and a currency warning. Corrected: deleting an event with sales is refused (Cancel event instead), a partial refund frees no waitlist spot and keeps a wallet pass valid, every method except cash leaves the embed frame, PayPal is no longer a payment-link example, "Payment URL" is the UI's "Payment Link", and the false line that cancelling or deleting a paid sale reminds you about the money is gone. Meta description 142. Coordinator: docs title override added so the tab and search result say what the page covers: "Selling Tickets: Payments, Refunds, Check-In - Event Schedule" (61). Coordinator (hosted email pass): the Sender & Compliance callout no longer implies sale notifications fall back to our address, the attendee-import email note says the schedule's own email settings are needed on eventschedule.com, and the feedback email-settings link now opens creating-schedules#integrations-email. |
| ✅ | ✅ | Subscriptions | `/docs/subscriptions` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Small exact edits: the Google Wallet line says every pass type saves as one undated pass, showing its admissions when more than one and archived after its expiry; the scanner table quotes its three refusal strings exactly; the refund notes separate a full refund (stops scanning, releases booked dates, drops off the Subscriptions tab) from a partial one (the pass keeps working). Everything else checked accurate and left alone. Coordinator: docs title override added so the tab and search result say what the page covers: "Subscriptions & Passes: Multi-Visit Tickets - Event Schedule" (60). Coordinator (hosted email pass): the booking-confirmation email is qualified as needing the schedule's own email settings on eventschedule.com. |
| ✅ | ✅ | Gift Cards | `/docs/gift-cards` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Accurate, so small edits: the Refunds callout keeps the still-true warning that a gift card's own Refund moves no money, contrasts it with ticket refunds, and says what a ticket refund does to a card (a full refund returns only the charged part and re-credits an Active card; an order the card covered in full shows Mark as Refunded; a partial refund leaves the balance alone). The Actions table notes that Refund moves no money. Coordinator: docs title override added so the tab and search result say what the page covers: "Gift Cards: Sell and Redeem Balances - Event Schedule" (53). |
| ✅ | ✅ | Allocated Seating | `/docs/allocated-seating` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. Reviewed; the 12-minute hold verified against `SeatHoldService::HOLD_SECONDS`. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: The box-office refund advice went wrong once refunds shipped: releasing a seat moves no money and leaves the order paid, so the page now says to return the seat's share as a partial refund from Sales (Stripe or PayPal) and warns that Mark as Refunded releases every seat on the order. The Aug 27 additions quote their real labels (Choose your seats, How many seats?, Find best seats, List, Select the whole row, Release N seats, Arrived, How the run is selling), the Date picker on the console, designer and report is covered, and the non-existent "Choose your own seats" label is gone. The "Run the door" search row was updated. Coordinator: docs title override added so the tab and search result say what the page covers: "Allocated Seating: Seat Maps and Box Office - Event Schedule" (60). |
| ✅ | ✅ | Appointments | `/docs/appointments` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. Reviewed; already correct that booking is on every plan and the plan governs how many types. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Refunds now move money, so the old "How refunds work" callout became a Refunds sub-section (#refunds, nav link and search row): Refund Ticket on the Sales page, where a full Stripe refund cancels the booking, frees the time and sends the guest the cancellation email, a partial one leaves it Confirmed, payment-link or cash bookings show Mark as Refunded, and you refund before cancelling because a refund made in Stripe's own dashboard is not reported back. Invoice Ninja joined the methods appointments never take, "Refunded" became "fully refunded" in the Cancelled status, the reschedule rules and Cancel appointment, and the page says a booking gets no QR code or Google Wallet pass and that booking guests are in the Ticket Buyers newsletter segment. The meta description names the three payment methods and refunds. Coordinator: docs title override added so the tab and search result say what the page covers: "Appointments: Booking, Payments, Refunds - Event Schedule" (57). Coordinator (hosted email pass): Good to know adds that without email settings you get no booking notices either. |
| ✅ | ✅ | Event Graphics | `/docs/event-graphics` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. Reviewed; template-variable list matches `EventTextGenerator::parseTemplate()`. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: The page badged the generator Pro, but GraphicController has had no plan check since 0540a6b39: the hero badge and plan callout now say Free on every plan, with only the AI text prompt and scheduled graphic emails Enterprise. Added that every hosted graphic carries an eventschedule.com credit bottom right on any plan and a selfhosted one carries none. The template-variable tables were re-checked against EventTextGenerator::parseTemplate() and the Overlay Text subset against parseOverlayText(): exact, and the twin list on creating-schedules agrees. The meta description leads with "free on every plan". Coordinator: docs title override added so the tab and search result say what the page covers: "Event Graphics: Shareable Schedule Images - Event Schedule" (58). |
| ✅ | ✅ | Newsletters | `/docs/newsletters` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. The "nothing goes out automatically" line was corrected: `app:send-event-announcements` is a separate rail that does not spend the allowance. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: New "Three Lists, Three Kinds of Mail" table (email subscribers, account followers and an event's interest list: how each joins, what each gets automatically, whether a newsletter reaches it), the hosted Follow pop-up as a fourth sign-up place, and the Email subscribers segment type the Creating a Segment steps left out. Corrected: email settings or a verified phone are needed only above 50 recipients, and the digest follows the same rule; the digest is at most one per 72 hours, only events the schedule created, password-protected events excluded, up to 25 per digest; All Followers excludes confirmed subscribers; Waitlist includes free RSVP waitlists on every plan; Ticket Buyers includes booking guests and cancelled or refunded orders. The admin-only Offer block came out of the Block Types search row. Coordinator: docs title override added so the tab and search result say what the page covers: "Newsletters: Builder, Segments, A/B Tests - Event Schedule" (58). |
| ✅ | ✅ | Analytics | `/docs/analytics` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. Two claims that scanning tickets is a Pro feature were corrected (`AnalyticsController` has no plan check at all), and the Check-Ins tab badge moved from Pro to Free. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Documented the new Broken links panel (#web-broken-links: addresses that match no event, top ten, single schedule only) and rewrote Social Link Clicks around the short address every link now has (per platform or site, crawler filter, ten clicks a day per visitor, owner and team clicks skipped). Corrected the view cap (10 per visitor per schedule plus 3 per visitor per event), revenue (gross of partial refunds, includes paid appointment bookings) and the Check-Ins tab (includes upcoming events, reads paid and undeleted sales, fills in without scans, counts bookings as no-shows). "Browsers" came out of the Web Analytics search row; the dashboard has no browser chart. Coordinator: docs title override added so the tab and search result say what the page covers: "Analytics: Views, Revenue and Check-Ins - Event Schedule" (56). |
| ✅ | ✅ | Account Settings | `/docs/account-settings` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Four stale claims fixed: Invoice Ninja marks a sale paid through the webhook it registers on connect; Stripe takes payment on Stripe's own hosted checkout page (the page said the buyer never left the checkout); a newsletter to 50 people or fewer no longer needs a verified phone; hosted exports are not free of personal data. Added which methods refund through the provider (Stripe and PayPal; Mark as Refunded for the rest), Stripe as the only installments method, the full backup contents, where per-schedule notification emails live, and Payfast and PayPal in the in-page nav. Title 59, description 153; the Payment Methods, Payment Link (was "Payment URL") and Backup & Restore search rows updated. |
| ✅ | ✅ | Boost | `/docs/boost` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: The call-to-action row now follows MetaAdsService exactly (a ticketed hybrid event also gets Get Tickets; Learn More is for an event with no tickets set up), a new Destination row says every click lands on the event's own page, which Advanced Mode cannot change, and a tip covers boosting before tickets are on sale by pointing at the free interest list. The 15-minute stats sync, 30-minute pending timeout, next-day settlement, refund rules and spending limits were re-checked and left alone. Title 56. |
| ✅ | ✅ | AI Import | `/docs/ai-import` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: New "New Pages and Requests" section (#new-pages, nav link and search row), which the page never covered: a performer or venue the import did not match gets a public, noindexed, claimable page on save (an existing account with that email owns it at once), and an act or venue that runs its own schedule gets the event as a request unless you are on its approved list. The rate-limit quote is now the exact string, visitors' submissions count against the allowance, and Auto Import is described as a daily import from event URLs, not a city search. Title 53, description 142; the daily-allowance table was already right. |
| ✅ | ✅ | Scan Agenda | `/docs/scan-agenda` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Re-verified and accurate. Added that the 10-a-day agenda allowance is its own counter, separate from the AI import allowance (Enterprise's 100 imports a day do not apply), with the exact refusal message. Title 58 and description 138 now say what the page does, and the HowTo description matches. |
| ✅ | ✅ | Referral Program | `/docs/referral-program` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Every reward rule re-checked against ReferralController, ProcessReferralCredits and SubscriptionController and found right. New rule: the first referral link in a browsing session keeps the credit. The Expired row said cancelling expires a referral; only a subscription that has actually ended does (the grace period still counts). Title 51, description 145. |

## Docs - Selfhost (12)

| Reviewed | Updated | Page | URL | Notes |
|:--------:|:-------:|------|-----|-------|
| ✅ | ✅ | Selfhost Overview | `/docs/selfhost` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. The card grid sat straight under an h2, so the shared component now takes a heading level instead of hardcoding h4. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: The "No plan gates" callout listed ticketing, QR check-in and event graphics among the paid badges a selfhost install escapes, though all three are free; it now names what is actually gated (check-in dashboard, passes, gift cards, installments, promo codes, add-ons, ticket waitlist, scheduled graphic emails and the rest). The payments step no longer implies Stripe Connect works single-tenant and covers PayPal and Payfast, the differences table gains a Payments row, Auto Import reads as a daily import from a URL list whose city list only filters, Google Wallet covers free registrations, the admin line says it edits any schedule, and the meta description names PayPal, Payfast and Google Wallet. Coordinator: docs title override added so the tab and search result say what the page covers: "Selfhost Guide: Run Event Schedule on Your Own Server" (53, was "Selfhost Documentation - Event Schedule", 39). |
| ✅ | ✅ | Installation | `/docs/selfhost/installation` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: The "Every feature is included" callout listed free features (ticketing, check-in, event graphics) as what the Enterprise resolution buys; it now lists what hosted keeps for Pro and Enterprise. The scheduler table gains the hourly jobs shipped since the last pass (new-event digest, interest-list emails, installment charges) and the five-minute curator-source sync. Spam protection no longer says Turnstile guards every public form (the sign-up panel and the "Notify me" list use a honeypot and rate limits). New title (59) and description; the Requirements search row's stale "PHP 8.1+" is now 8.2+. |
| ✅ | ✅ | Stripe (Selfhost) | `/docs/selfhost/stripe` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Now titled and headed for what it covers (Stripe, PayPal and Payfast on a selfhost install), with a new Refunds section (#refunds, nav and search row): Refund Ticket on the Sales page sends Stripe money back with the current .env keys and PayPal money from the owner's account, then the install-wide one, in full or in part; every other method gets Mark as Refunded, dashboard refunds are never reported back, and no refund email is sent. Installment plans share the Stripe keys and rely on the cron, PayPal and Payfast are scoped to ticket orders, the hosted Pro list drops "custom checkout fields" (ticket-type questions are free), and an amount-mismatch sale points to Refund on the admin Revenue page. |
| ✅ | ✅ | Google Calendar (Selfhost) | `/docs/selfhost/google-calendar` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: The queue advice was wrong both ways: the bulk resync is queued in batches of ten, so with QUEUE_CONNECTION=database the scheduler's minutely worker finishes it with no separate queue:work, but on the shipped sync setting it never gets past the first ten events (reported as a code defect). The page points at the commented Google block in .env.example, says a calendar deletion handled as "Mark as cancelled" notifies nobody (unlike Cancel event in the editor), and has a new title and description. |
| ✅ | ✅ | Microsoft Calendar (Selfhost) | `/docs/selfhost/microsoft-calendar` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: The webhook queue callout no longer demands a standing queue:work: with QUEUE_CONNECTION=database the scheduler's minutely worker runs each inbound sync within about a minute, and the troubleshooting line now blames a stopped cron. The page points at the commented Microsoft block in .env.example and says a "Mark as cancelled" triggered by an Outlook deletion notifies nobody; the new title and description name Microsoft 365 and Teams. |
| ✅ | ✅ | Boost (Selfhost) | `/docs/selfhost/boost` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Already accurate against FEATURES.md Monetization and the scheduler, so small edits: the Pixel paragraph said the server-side Purchase conversion fires only for Stripe sales; it now names every payment path that settles through the app, reports free registrations at zero value, and says hand-marked sales send nothing. Replaced the generic "Learn how to" meta description and added a specific title (56). |
| ✅ | ✅ | Admin (Selfhost) | `/docs/selfhost/admin` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Scoped the "Hosted installs only" callout to plan assignment: /admin/schedules is on every install, and the section now covers the Claimed / Unclaimed / All owners filter, curator approved lists rewritten on rename and cleared on release, Release Subdomain for a deleted schedule that still holds its name, and Restore as the undo for a "This is not me" or API takedown. The Needs attention table was rebuilt from AdminAlertService (five missing rows added: scheduled tasks not running, jobs not draining, unrecognized subscriptions, refunds awaiting confirmation, update available; the unverified and referral rows removed from the app in August dropped). Corrected the Overview availability rows and the settings-card count (six cards, three on a plain selfhost); the mismatch Refund reads "Stripe or PayPal", with a new Refunds Awaiting Confirmation subsection and a note that partially refunded sales stay in the totals. Specific title (52) and meta description. |
| ✅ | ✅ | Federation (Selfhost) | `/docs/selfhost/federation` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Content changes live in the shared federation partial (also rendered by /docs/saas/federation): the "never sent" line uses the current visibility labels (Draft, Internal, Unlisted); "a listing carries every participant's name" was untrue and now describes what a listing carries; auto-created unclaimed pages neither list an event nor veto it; the sync paragraph lists every way an event stops qualifying, including its schedule being deleted. The shell's title and meta were already specific. Coordinator: four more verified fixes to the shared partial: the per-schedule control is a three-choice setting (Not decided yet, Listed on the network, Not listed), every schedule including pre-existing ones starts undecided (migration 2026_07_28_000001 reset them), the connection status reads Pending, recurring events send their next three dates within 120 days, and listings link to the event's address on the install, never a custom domain. |
| ✅ | ✅ | Email (Selfhost) | `/docs/selfhost/email` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. The newsletter description now covers both outbound rails, so an install with no mail settings knows it sends neither. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: New What Is Sent Automatically section (#what-is-sent, nav link and search row) listing each automatic email and what it needs: the new-event digest (the schedule's own events only, checked hourly, at most one every 72 hours), the interest list's on-sale email and 48-hour reminder, its cancellation notice and change notices only when the organizer chooses to notify, the sign-up confirmation (an account only with ALLOW_REGISTRATION=true), plus two plain statements: a selfhost never emails buyers about a change or cancellation (that needs a schedule's own SMTP) and claim invitations are hosted-only; the four timing and batch env vars. Corrected the MAIL_MAILER=log callout (the digest, on-sale and reminder emails are skipped; confirmations and interest change and cancellation notices go to the log; the forms keep collecting) and updated the sender section ("Schedule via name" display name and Reply-To), Testing and Troubleshooting. New title (53) and meta description. Coordinator: the manifest blurb (docs index card and nav) no longer lists Mailgun as a mail driver; it is supported only through SMTP. |
| ✅ | ✅ | AI (Selfhost) | `/docs/selfhost/ai` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Renamed the auto import heading to "Daily Auto Import from event URLs" so it no longer reads as a city search, stated that both eventschedule.com daily allowances (event parsing and generated images) are off on selfhost, and added that venues and performers an import names become ownerless claim pages listed under Unclaimed, with no claim invitation sent on selfhost. New title (58) and meta description. |
| ✅ | ✅ | Google Wallet (Selfhost) | `/docs/selfhost/google-wallet` | Added 2026-09-07 (`a51962a8c`) with the Add to Google Wallet feature (#122); new to the tracker this round, so it has no earlier record. ROUND 3: First review, end to end against GoogleWalletService, docs/GOOGLE_WALLET_SETUP.md and the .env.example block. Corrected: free registrations get a pass; the badge sits in its own row, not under the QR code; a pass expires three hours after the event ends; the guest count is dropped with the notes; the event name never counts against the link cap (so "shorten the event name" was wrong advice); a branding change never reaches an existing class. Added: one pass per event on multi-event orders and one per season pass, the Event Schedule logo fallback, the exact error text, re-saving venues after adding BACKEND_GOOGLE_KEY, the prefix character rules, and the "fully refunded" distinction at the door. Coordinator: docs/GOOGLE_WALLET_SETUP.md, which asks to be kept in step with this page, was brought up to date with the same corrections. |
| ✅ | ✅ | Accessibility (Selfhost) | `/docs/selfhost/accessibility` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Configuration said five settings and gave ACCESSIBILITY_LAST_REVIEWED a default of 2026-05-03; config/accessibility.php now has seven, so ACCESSIBILITY_PUBLIC_PAGES_MEASURED and ACCESSIBILITY_PUBLIC_MEASUREMENT_DATE were added (they describe eventschedule.com's own measurement), and the three dated or counted defaults now render from config so they cannot go stale. Otherwise accurate. |

## Docs - SaaS (5)

| Reviewed | Updated | Page | URL | Notes |
|:--------:|:-------:|------|-----|-------|
| ✅ | ✅ | SaaS Setup | `/docs/saas` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: The Stripe section described ticket money as Stripe Connect only; it now says each tenant's sales settle into their own Stripe (Connect), PayPal, Payfast or Invoice Ninja account, a payment link or cash, with no platform fee, and that refunds go back through that same account. Added an optional Google Wallet Passes subsection (#google-wallet, with a search row). The demo callout gives the real admin path (Manage > Schedules, Plan Type) and notes the demo schedule never shows ads; the admin panel docs are linked; the meta description was rewritten for white-label SaaS searches. Coordinator: title "White-Label SaaS Setup: Run Your Own Platform - Event Schedule" (62, was 27). Coordinator (hosted email pass): the mail prerequisite says that in hosted mode sale notifications, appointment and gift card emails, pass booking confirmations and buyer change notices go only through a tenant schedule's own email settings. |
| ✅ | ✅ | Custom Domains (SaaS) | `/docs/saas/custom-domains` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Added what an active custom domain carries beyond the page itself: event links in newsletters, the new-event digest and interest-list and waitlist mail land on it while ticket links stay on the app subdomain, and the live calendar feed and each schedule's short-link addresses answer on it. Corrected drifted details: the reserved-host rule also refuses the operator's own base domain, Re-provision is a single app-spec write, a refused Remove clears nothing, missing DigitalOcean credentials leave a domain with no status rather than pending, a deleted schedule's domain 404s; the troubleshooting heading quotes the real error. Coordinator: title "Custom Domains for Tenants: DNS and SSL - Event Schedule" (56, was 31). |
| ✅ | ✅ | Twilio (SaaS) | `/docs/saas/twilio` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: SMS invitations are described as they ship (a phone-only team invite gets a texted sign-up link; an organizer listing a phone-only act or venue can text it a claim link), a verified phone is shown as what lets someone claim a phone-only page, "platform discovery" became search indexing plus federation eligibility, and the editor requirement and save-failure reply were added. The reviewer removed the Enterprise badge from WhatsApp event creation because WhatsAppWebhookController has no plan check; the coordinator restored it, so the page matches pricing and every other surface, and flagged the missing gate as a product bug (the FEATURES.md row now says so). Coordinator: title "Twilio: SMS, WhatsApp and Phone Verification - Event Schedule" (61, was 35). |
| ✅ | ✅ | Federation (SaaS) | `/docs/saas/federation` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: The SaaS shell's title, description and lede were re-verified against FederationService and left unchanged. The shared body (partials/federation-content) was corrected by the selfhost reviewer, and the coordinator then applied four more verified fixes: the per-schedule control is a three-choice setting (Not decided yet, Listed on the network, Not listed), every schedule including pre-existing ones starts undecided (migration 2026_07_28_000001 reset them), the connection status reads Pending, recurring events send their next three dates within 120 days, and listings link to the event's address on the install, never a custom domain. |
| ✅ | ✅ | Monetization (SaaS) | `/docs/saas/monetization` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: The promotion budget is also capped by the schedule's boost spending limit (10 by default on hosted, raised only by completed Meta boosts or an admin's Set Limit); the page now says so and where to raise it. Also: unspent budget is refunded a day after the campaign ends, a promotion stops within the five-minute cache window, the per-click pause (0.02% after 5,000 views) and the per-IP billing cap (100 a day), the demo schedule and unclaimed pages never show ads, a rejected credit-paid promotion is refunded to credit, six undocumented env variables; the meta description names all three earners. Coordinator: title "SaaS Monetization: Ads, Promotions and Stay22 - Event Schedule" (62, was 29). |

## Docs - Developer (2)

| Reviewed | Updated | Page | URL | Notes |
|:--------:|:-------:|------|-----|-------|
| ✅ | ✅ | API | `/docs/developer/api` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: Corrected the plan exceptions (Delete Schedule, both List Categories routes and List Fan Content have no plan check, alongside Create Schedule and Create Sale) and the rate limits (counted per IP, shared by every key on that address, not a rolling window). The refund reference gained the idempotency_key format, amount ignored on record-only methods, the stricter refund permission, dashboard refunds never reaching the app, a partial-refund example, and correct 403 and cascade rules for guest rows and multi-event orders; also documented the currency lock, seat assignment and seats, the secret rule, new-schedule verification with no trial, and the subdomain release on delete. Endpoint paths fixed in nine search rows. Coordinator: title "REST API Reference: Schedules, Events, Sales - Event Schedule" (61, was 35). |
| ✅ | ✅ | Webhooks | `/docs/developer/webhooks` | Accuracy pass (shared docs shell by design). 2026-09 review: plan badges checked against the gate in code, every heading-level skip fixed, and all 418 doc-search anchors plus 110 Help-button deep links verified to resolve. PASS 2: the manual now says how current each page is and where to fix it - a Last updated date read from the SAME config/sitemap_lastmod entry the page's JSON-LD dateModified already used (so the two cannot disagree) and a link to that page's own view file in the public repo. Re-verified in this pass: every plan badge on the page against docs/FEATURES.md, every in-page and cross-page fragment link (2,406 site-wide, 0 dangling), contrast in both modes, tap targets and mobile overflow. ROUND 3: The delivery table promised up to three attempts with backoff; deliveries are sent once, after the response, and never retried (dispatchAfterResponse runs on the sync connection), so the table, the 4xx/5xx advice and the duplicates rationale say so and point to reconciling through the Sales API. Also: "twelve" types became fourteen, deliveries go to the account that created the event, sale.paid lists PayPal, Payfast, payment links, installments and admin approval, sale.refunded fires on a full refund only, sale.cancelled and event.updated say exactly what fires them, unchecking every type subscribes to all, installment and seats payload fields, the secret rule, and the Node sample no longer throws on a missing header. Coordinator: title "Webhooks: Signed Sale and Event Notifications - Event Schedule" (62, was 25). |

## Dynamic pages (not counted in the 151)

| Reviewed | Updated | Page | URL | Notes |
|:--------:|:-------:|------|-----|-------|
| |   | Blog | `/blog` | Dynamic / DB-driven index (no static Blade view); listed for completeness PASS 2: re-verified the SEO work the index carries (page 2+ self-canonicalizing and indexable, filtered views noindexed, per-page titles and descriptions, JSON-LD emitted through SeoUtils) and confirmed the view counter writes through withoutTimestamps, so reading a post no longer moves its updated_at. Contrast, targets and overflow measured clean. |
